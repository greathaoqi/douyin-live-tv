---
phase: 02-authentication-api-layer
plan: 4
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - DouyinLiveTV/UI/Login/LoginView.swift
  - DouyinLiveTV/UI/Login/LoginViewModel.swift
  - DouyinLiveTV/UI/Common/ContentView.swift
  - DouyinLiveTV/App/DouyinLiveTVApp.swift
autonomous: false
requirements: [AUTH-01, AUTH-03]
user_setup: []

must_haves:
  truths:
    - "LoginView displays QR code when available"
    - "QR code has accent color border and meets minimum size 280x280pt"
    - "User can tap Start Login to begin QR login flow"
    - "Loading indicator shown while polling for scan confirmation"
    - "Errors displayed clearly to user"
    - "Root ContentView routes to LoginView when unauthenticated, main UI when authenticated"
    - "App checks auth state on launch"
  artifacts:
    - path: "DouyinLiveTV/UI/Login/LoginView.swift"
      provides: "SwiftUI login screen with QR display per UI spec"
    - path: "DouyinLiveTV/UI/Login/LoginViewModel.swift"
      provides: "View model coordinating with AuthService for login flow"
    - path: "DouyinLiveTV/UI/Common/ContentView.swift"
      provides: "Root view with conditional routing based on auth state"
    - path: "DouyinLiveTV/App/DouyinLiveTVApp.swift"
      provides: "App entry point that checks auth on launch"
  key_links:
    - from: "LoginViewModel"
      to: "AuthService"
      via: "calls startQRLogin and polls"
      pattern: "authService.startQRLogin"
    - from: "LoginView"
      to: "QRCodeGenerator"
      via: "generates UIImage from QR URL"
      pattern: "generateQRCode"
    - from: "ContentView"
      to: "AuthStateManager"
      via: "observes state for routing"
      pattern: "authStateManager.state"
    - from: "DouyinLiveTVApp"
      to: "AuthStateManager"
      via: "calls checkStoredCredentials on launch"
      pattern: "checkStoredCredentials"
---

<objective>
Build the login screen UI according to the UI specification, integrate authentication state routing at app launch, and connect to AuthService.

Purpose: Provides the user-facing authentication flow where user can see the QR code, scan with their mobile Douyin app, and complete login. Implements the visual contract from 02-UI-SPEC.md.
Output: LoginView with QR display, LoginViewModel, root routing based on auth state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-api-layer/02-CONTEXT.md
@.planning/phases/02-authentication-api-layer/02-RESEARCH.md
@.planning/phases/02-authentication-api-layer/02-UI-SPEC.md
@DouyinLiveTV/App/AuthStateManager.swift
@DouyinLiveTV/Data/Network/AuthService.swift
@DouyinLiveTV/Data/Network/QRCodeGenerator.swift
@DouyinLiveTV/Domain/Models/LoginQRCode.swift
@DouyinLiveTV/App/DependencyContainer.swift
</context>

<interfaces>
From AuthStateManager:
```swift
@MainActor
class AuthStateManager: Observable {
    enum State {
        case unauthenticated
        case authenticating
        case authenticated
    }
    var state: State { get }
    func checkStoredCredentials()
    func logout()
}
```

From AuthService:
```swift
actor AuthService {
    func startQRLogin() async throws -> LoginQRCode
}
```

From QRCodeGenerator:
```swift
func generateQRCode(from string: String, size: CGSize) -> UIImage?
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LoginViewModel coordinating with AuthService</name>
  <files>C:/Users/59213/douyin/DouyinLiveTV/UI/Login/LoginViewModel.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/AuthService.swift
    - C:/Users/59213/douyin/DouyinLiveTV/App/AuthStateManager.swift
  </read_first>
  <action>
    1. Create UI/Login/ directory if it doesn't exist
    2. Create LoginViewModel.swift:
       - `@MainActor class LoginViewModel: Observable`
       - Published properties:
         - qrImage: UIImage?
         - state: ViewState = .idle (enum: idle, loading, error(message: String))
         - hasQR: Bool { qrImage != nil }
       - Private dependencies:
         - let authService: AuthService
         - let authStateManager: AuthStateManager
       - Public init: receives dependencies from DependencyContainer
       - Implement public func startLogin() async
         - Sets state to .loading
         - Calls authService.startQRLogin()
         - Uses generateQRCode to create UIImage (size 280x280pt)
         - Updates published qrImage and state to .idle
         - Catches errors and sets state to .error with message per copy contract in 02-UI-SPEC.md
    3. Use the copy from 02-UI-SPEC.md for error messages:
       - Network error: "Could not connect to Douyin. Check your internet connection and try again."
       - Login expired: "Your login session has expired. Please scan the QR code again to log in."
       - Login failed: "Login process failed. Please generate a new QR code and try again."
  </action>
  <verify>
    <automated>ls C:/Users/59213/douyin/DouyinLiveTV/UI/Login/LoginViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - LoginViewModel.swift exists at UI/Login/LoginViewModel.swift
    - LoginViewModel is @MainActor and @Observable
    - Contains published qrImage: UIImage? property
    - Contains published state property with idle/loading/error cases
    - Contains `func startLogin() async` that calls `authService.startQRLogin()`
    - Calls `generateQRCode` with size 280x280
    - Error messages match 02-UI-SPEC.md text exactly
  </acceptance_criteria>
  <done>LoginViewModel created with coordination between UI and AuthService</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create LoginView SwiftUI per UI specification</name>
  <files>C:/Users/59213/douyin/DouyinLiveTV/UI/Login/LoginView.swift</files>
  <read_first>
    - C:/Users/59213/douyin/.planning/phases/02-authentication-api-layer/02-UI-SPEC.md
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Login/LoginViewModel.swift
  </read_first>
  <action>
    Create LoginView.swift in UI/Login/:
    - Define struct LoginView: View with view model from environment/DependencyContainer
    - Layout matches 02-UI-SPEC.md visual hierarchy:
      1. Heading: "Douyin Live Login" (Display size 60px, semibold) at top
      2. QR code: center, 280x280pt minimum, accent color border per 02-UI-SPEC.md
      3. Instructions: "Scan the QR code with your Douyin mobile app to log in." (Body size 28px)
      4. "Start Login" button at bottom (Primary CTA)
      5. If error state, show error message in red
      6. If loading, show ProgressView along with text "Waiting for scan..."
    - Spacing follows 02-UI-SPEC.md scale: 3xl (64px) page spacing, xl (32px) between sections
    - All interactive elements (Start Login button) must be 88x88pt minimum size per tvOS HIG (from UX-07 requirement)
    - Colors use system colors that adapt to dark mode (per 02-UI-SPEC.md):
      - Background: systemBackground
      - QR container: secondarySystemBackground
      - Accent: accentColor
      - Errors: systemRed
    - On tap of Start Login, task runs that calls viewModel.startLogin()
    - All content fits within safe area/padding to avoid overscan cutting (per UX-06)
  </action>
  <verify>
    <automated>ls C:/Users/59213/douyin/DouyinLiveTV/UI/Login/LoginView.swift</automated>
  </verify>
  <acceptance_criteria>
    - LoginView.swift exists at UI/Login/LoginView.swift
    - Contains "Douyin Live Login" heading with 60px font
    - QR code image displayed at minimum 280x280pt with accent color border
    - Instructions text matches "Scan the QR code with your Douyin mobile app to log in."
    - "Start Login" button exists at bottom
    - Start Login button meets 88x88pt minimum focus size
    - Loading state shows ProgressView
    - Error state shows error message in systemRed
    - All content contained within safe area
    - Uses system adaptive colors (supports dark mode automatically)
    - No scroll needed - all content fits on one screen
  </acceptance_criteria>
  <done>LoginView created matching 02-UI-SPEC.md visual contract</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrate auth state routing into ContentView and app entry point</name>
  <files>
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Common/ContentView.swift
    - C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift
  </files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Common/ContentView.swift
    - C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift
    - C:/Users/59213/douyin/DouyinLiveTV/App/AuthStateManager.swift
  </read_first>
  <action>
    1. Update ContentView.swift:
       - Inject AuthStateManager from DependencyContainer
       - Switch on authStateManager.state:
         - unauthenticated: show LoginView()
         - authenticating: show LoginView()
         - authenticated: show placeholder "Main UI" text (real main UI will come in later phases)
       - This provides the routing that downstream phases will expand

    2. Update DouyinLiveTVApp.swift:
       - Get AuthStateManager from DependencyContainer
       - In onAppear, call authStateManager.checkStoredCredentials()
       - This implements AUTH-03 requirement: session persists across app restarts - on launch we check Keychain for existing valid tokens

    Follow D-03: credentials only from Keychain, no caching elsewhere.
  </action>
  <verify>
    <automated>grep -q "unauthenticated" C:/Users/59213/douyin/DouyinLiveTV/UI/Common/ContentView.swift && grep -q "checkStoredCredentials" C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift</automated>
  </verify>
  <acceptance_criteria>
    - ContentView imports AuthStateManager
    - ContentView switches views based on authStateManager.state
    - unauthenticated displays LoginView
    - DouyinLiveTVApp calls checkStoredCredentials on appear
    - No tokens stored outside Keychain per D-04
  </acceptance_criteria>
  <done>Root routing integrated and app launch checks for stored credentials</done>
</task>

</tasks>

<verification>
After tasks complete, app builds successfully. The checkpoint below verifies the UI works correctly.
</verification>

<success_criteria>
1. LoginViewModel coordinates with AuthService ✓
2. LoginView displays QR code per 02-UI-SPEC ✓
3. All focus targets meet minimum 88x88pt size ✓
4. Colors adapt to dark mode automatically ✓
5. ContentView routes correctly based on auth state ✓
6. App checks stored credentials on launch (AUTH-03) ✓
7. App builds with no errors ✓
</success_criteria>

<checkpoint>
<checkpoint:human-verify gate="blocking">
  <what-built>Complete authentication UI with QR display and app-level routing</what-built>
  <how-to-verify>
    1. Build and run the app in tvOS simulator: `open DouyinLiveTV.xcodeproj` then run
    2. On launch, app should display the login screen with "Douyin Live Login" heading
    3. Verify the "Start Login" button is focusable with Siri Remote (larger when focused)
    4. Click "Start Login" - a QR code should appear
    5. Verify QR code is at least 280pt and has accent border
    6. Verify instructions text is visible and readable
    7. Verify all content fits on screen (nothing cut off by overscan)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues</resume-signal>
</checkpoint:human-verify>
</checkpoint>

<output>
After completion and human approval, create `.planning/phases/02-authentication-api-layer/02-04-SUMMARY.md`
</output>
