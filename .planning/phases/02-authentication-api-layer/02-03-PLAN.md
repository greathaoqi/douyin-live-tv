---
phase: 02-authentication-api-layer
plan: 3
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - DouyinLiveTV/Data/Network/AuthService.swift
  - DouyinLiveTV/App/AuthStateManager.swift
  - DouyinLiveTV/App/DependencyContainer.swift
  - DouyinLiveTVTests/AuthServiceTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-04]
user_setup: []

must_haves:
  truths:
    - "AuthService can start QR login flow and poll for confirmation"
    - "AuthStateManager observes authentication state for UI routing"
    - "App checks for stored credentials on launch"
    - "401 responses trigger automatic token refresh"
    - "If refresh fails, user is logged out and prompted to re-login"
  artifacts:
    - path: "DouyinLiveTV/Data/Network/AuthService.swift"
      provides: "AuthService with QR login flow and polling, automatic token refresh"
      exports: ["actor AuthService"]
    - path: "DouyinLiveTV/App/AuthStateManager.swift"
      provides: "@MainActor AuthStateManager with observable auth state"
      exports: ["class AuthStateManager", "enum State"]
    - path: "DouyinLiveTV/App/DependencyContainer.swift"
      provides: "Dependencies registered (AuthStateManager, AuthService, APIClient, TokenStorage)"
  key_links:
    - from: "AuthService"
      to: "TokenStorage"
      via: "save and load tokens after login/refresh"
      pattern: "tokenStorage.save|tokenStorage.load"
    - from: "AuthService"
      to: "APIClient"
      via: "make API requests for QR login and status check"
      pattern: "apiClient.request"
    - from: "AuthService"
      to: "APIClient"
      via: "intercepts 401 errors and triggers token refresh"
      pattern: "unauthorized.*refresh"
    - from: "AuthStateManager"
      to: "TokenStorage"
      via: "checks stored credentials on app launch"
      pattern: "checkStoredCredentials"
    - from: "DependencyContainer"
      to: "All"
      via: "provides shared instances"
      pattern: "let authService|let apiClient|let tokenStorage|let authStateManager"
---

<objective>
Implement AuthService with QR login polling flow and automatic token refresh, plus AuthStateManager for UI observation. Register all dependencies in DependencyContainer.

Purpose: This implements the core authentication business logic including the complete QR login flow (per D-01/D-02) and automatic token refresh (per D-07/D-08). AUTH-01, AUTH-03, and AUTH-04 requirements are addressed here.
Output: AuthService, AuthStateManager, updated DependencyContainer with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-api-layer/02-CONTEXT.md
@.planning/phases/02-authentication-api-layer/02-RESEARCH.md
@DouyinLiveTV/Data/Keychain/TokenStorage.swift
@DouyinLiveTV/Data/Network/APIClient.swift
@DouyinLiveTV/Data/Network/Endpoint.swift
@DouyinLiveTV/Domain/Models/AuthToken.swift
@DouyinLiveTV/Domain/Models/LoginQRCode.swift
@DouyinLiveTV/App/DependencyContainer.swift
</context>

<interfaces>
From TokenStorage.swift (after 02-01):
```swift
public class TokenStorage {
    public func save(_ token: AuthToken) throws
    public func load() -> AuthToken?
    public func clear()
    public func hasValidTokens() -> Bool
}
```

From APIClient.swift (after 02-02):
```swift
public class APIClient {
    public func request<T: Decodable>(_ endpoint: Endpoint<T>) async throws -> T
}
public enum APIError: Error, Equatable {
    case unauthorized
}
```

From DependencyContainer.swift:
```swift
class DependencyContainer {
    static let shared = DependencyContainer()
    let modelContainer: ModelContainer
    private init() { ... }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement AuthService with QR login flow and polling</name>
  <files>C:/Users/59213/douyin/DouyinLiveTV/Data/Network/AuthService.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Keychain/TokenStorage.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/APIClient.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/AuthToken.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LoginQRCode.swift
  </read_first>
  <action>
    Create AuthService.swift in Data/Network/:

    - Define public actor AuthService (actor prevents race conditions for concurrent requests, per research pitfall 3)
    - Private properties:
      - let tokenStorage: TokenStorage
      - let apiClient: APIClient
      - weak var authStateManager: AuthStateManager?
    - Public init: tokenStorage, apiClient, authStateManager
    - Implement public func startQRLogin() async throws -> LoginQRCode
      - Calls getQRCode endpoint via APIClient
      - Starts background polling task using the pattern from 02-RESEARCH.md
      - Returns the LoginQRCode for UI display
    - Implement private func pollForConfirmation(uuid: String) async throws
      - Poll every 2.5 seconds (per research recommendation)
      - Maximum polling time: 2 minutes (48 attempts)
      - Stops when confirmed or timeout/error
      - When confirmed: saves tokens to TokenStorage, updates auth state to .authenticated
    - Follow locked decisions: QR code only login (D-01, D-02) — no other login methods supported
    - Polling stops on timeout and throws error, UI must generate new QR code
  </action>
  <verify>
    <automated>ls C:/Users/59213/douyin/DouyinLiveTV/Data/Network/AuthService.swift</automated>
  </verify>
  <acceptance_criteria>
    - AuthService.swift exists at Data/Network/AuthService.swift
    - AuthService is defined as `public actor AuthService` (prevents race conditions)
    - Contains `func startQRLogin() async throws -> LoginQRCode`
    - Contains polling with 2.5 second interval between checks
    - Polling has maximum 2 minute timeout
    - On confirmation, calls `tokenStorage.save()` with received tokens
  </acceptance_criteria>
  <done>Actor-based AuthService with QR login flow and polling implemented</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement automatic token refresh logic</name>
  <files>C:/Users/59213/douyin/DouyinLiveTV/Data/Network/AuthService.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/AuthService.swift
  </read_first>
  <action>
    Add token refresh capability to AuthService:
    - Implement public func refreshIfNeeded() async throws -> AuthToken?
      - Checks if current access token is expired
      - If not expired, returns current token
      - If expired, uses refresh token to get new access/refresh token pair from Douyin API
      - Saves new tokens to TokenStorage
      - Returns new tokens
    - Implement public func authenticatedRequest<T: Decodable>(_ endpoint: Endpoint<T>) async throws -> T
      - Gets valid token (refreshes if needed)
      - Makes request with authorization header
      - If 401 error occurs after refresh, clears tokens and sets auth state to unauthenticated
      - This implements the "request interceptor + retry" pattern from 02-RESEARCH.md
      - Follows locked decision: automatic silent refresh (D-07), only logout if refresh fails (D-08)
    - Use actor isolation to prevent concurrent refresh attempts (eliminates race condition per pitfall 3)
  </action>
  <verify>
    <automated>grep -q "authenticatedRequest" C:/Users/59213/douyin/DouyinLiveTV/Data/Network/AuthService.swift && grep -q "refreshIfNeeded" C:/Users/59213/douyin/DouyinLiveTV/Data/Network/AuthService.swift</automated>
  </verify>
  <acceptance_criteria>
    - AuthService contains `func authenticatedRequest<T: Decodable>(_ endpoint: Endpoint<T>) async throws -> T`
    - AuthService contains `func refreshIfNeeded() async throws -> AuthToken?`
    - 401 after refresh triggers token clearing and state change to unauthenticated
    - Actor isolation prevents concurrent refresh race conditions
    - One retry attempt only per request (prevents infinite loops)
  </acceptance_criteria>
  <done>Automatic token refresh implemented with 401 interception</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create AuthStateManager and register dependencies</name>
  <files>
    - C:/Users/59213/douyin/DouyinLiveTV/App/AuthStateManager.swift
    - C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift
  </files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Keychain/TokenStorage.swift
  </read_first>
  <action>
    1. Create AuthStateManager.swift in App/:
       - Define `@MainActor class AuthStateManager: Observable, Observable` (use @Observable for observation)
       - Enum State: `case unauthenticated`, `case authenticating`, `case authenticated`
       - Private(set) var state: State = .unauthenticated
       - Private let tokenStorage: TokenStorage
       - Public init(tokenStorage: TokenStorage)
       - Implement public func checkStoredCredentials()
         - If hasValidTokens() → state = .authenticated
         - Else → state = .unauthenticated
       - Implement public func logout() clears tokens via tokenStorage.clear() and sets state = .unauthenticated
       - Implement public func transition(to: State) to update state
       - Must be @MainActor because UI observes it

    2. Update DependencyContainer.swift to register all new dependencies as shared instances:
       - Add public let tokenStorage: TokenStorage
       - Add public let apiClient: APIClient
       - Add public let authService: AuthService
       - Add public let authStateManager: AuthStateManager
       - Initialize all in init()
       - Keep the existing modelContainer initialization

    Follow D-04: tokens never stored anywhere except Keychain — AuthStateManager doesn't cache tokens, only state.
  </action>
  <verify>
    <automated>ls C:/Users/59213/douyin/DouyinLiveTV/App/AuthStateManager.swift && grep -q "tokenStorage" C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift</automated>
  </acceptance_criteria>
  <acceptance_criteria>
    - AuthStateManager.swift exists at App/AuthStateManager.swift
    - AuthStateManager is `@MainActor` with `@Observable`
    - AuthStateManager has `enum State` with three cases: unauthenticated, authenticating, authenticated
    - checkStoredCredentials() method exists
    - DependencyContainer has stored properties for tokenStorage, apiClient, authService, authStateManager
    - All four dependencies are initialized in DependencyContainer.init()
    - Tokens only stored in TokenStorage (Keychain), not cached elsewhere per D-04
  </acceptance_criteria>
  <done>AuthStateManager created and all dependencies registered in DependencyContainer</done>
</task>

</tasks>

<verification>
After this plan completes:
- Xcode builds with no errors
- All dependencies wired correctly
- QR login flow can be initiated
- 401 interception triggers refresh
- App checks stored credentials on launch
</verification>

<success_criteria>
1. Actor-based AuthService prevents race conditions ✓
2. QR login flow with polling implemented ✓
3. Automatic token refresh on 401 implemented ✓
4. Logout clears tokens from Keychain ✓
5. AuthStateManager provides observable state for UI ✓
6. Credentials checked on app launch ✓
7. All dependencies registered in DependencyContainer ✓
8. Follows D-07 and D-08 (automatic refresh, logout only when refresh fails) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-api-layer/02-03-SUMMARY.md`
</output>
