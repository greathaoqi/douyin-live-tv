---
phase: 02-authentication-api-layer
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - DouyinLiveTV/Data/Network/Endpoint.swift
  - DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift
  - DouyinLiveTV/Data/Network/APIClient.swift
  - DouyinLiveTV/Data/Network/QRCodeGenerator.swift
  - DouyinLiveTVTests/APIClientTests.swift
autonomous: true
requirements: [AUTH-01]
user_setup: []

must_haves:
  truths:
    - "Endpoint type defines Douyin API endpoints"
    - "APIClient can perform async HTTP requests with Codable parsing"
    - "QR code image can be generated from UUID using CoreImage"
    - "API client handles common error cases (network error, 401, 403)"
  artifacts:
    - path: "DouyinLiveTV/Data/Network/Endpoint.swift"
      provides: "Endpoint type for defining API requests"
      exports: ["struct Endpoint", "enum HTTPMethod"]
    - path: "DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift"
      provides: "Concrete Douyin API endpoint implementations for QR login"
      exports: ["extension Endpoint where Response == LoginQRCode", "extension Endpoint where Response == QRStatusResponse"]
    - path: "DouyinLiveTV/Data/Network/APIClient.swift"
      provides: "APIClient with async/await using native URLSession"
      exports: ["class APIClient", "enum APIError"]
    - path: "DouyinLiveTV/Data/Network/QRCodeGenerator.swift"
      provides: "QR code image generation using system CoreImage"
      exports: ["func generateQRCode(from:size:) -> UIImage?"]
  key_links:
    - from: "APIClient"
      to: "Endpoint"
      via: "request method that accepts Endpoint"
      pattern: "func request<T: Decodable>(_ endpoint: Endpoint<T>)"
    - from: "QRCodeGenerator"
      to: "CoreImage"
      via: "CIFilter CIQRCodeGenerator"
      pattern: "CIQRCodeGenerator"
---

<objective>
Create network layer with API client, endpoint definitions, and QR code generation.

Purpose: Provides the HTTP infrastructure needed for QR login flow and authenticated requests to Douyin. Uses native URLSession with async/await (no extra dependency per research recommendation). QR generation uses system CoreImage.
Output: Complete network layer with API client, endpoint type, Douyin-specific endpoints, and QR code generation with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-api-layer/02-CONTEXT.md
@.planning/phases/02-authentication-api-layer/02-RESEARCH.md
@.planning/phases/02-authentication-api-layer/02-UI-SPEC.md
@DouyinLiveTV/Domain/Models/AuthToken.swift
@DouyinLiveTV/Domain/Models/LoginQRCode.swift
</context>

<interfaces>
From DouyinLiveTV/Domain/Models/AuthToken.swift:
```swift
public struct AuthToken: Codable {
    public let accessToken: String
    public let refreshToken: String
    public let expiresAt: Date
}
```

From DouyinLiveTV/Domain/Models/LoginQRCode.swift:
```swift
public struct LoginQRCode: Codable {
    public let uuid: String
    public let qrURL: String
    public let expiresAt: Date
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Endpoint type and Douyin API endpoint definitions</name>
  <files>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/Endpoint.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift
  </files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LoginQRCode.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/AuthToken.swift
  </read_first>
  <action>
    1. Create directory Data/Network/ if it doesn't exist
    2. Create Endpoint.swift:
       - Define `enum HTTPMethod: String` with case get, post, put, delete
       - Define `struct Endpoint<Response: Decodable>` with:
         - path: String
         - method: HTTPMethod
         - parameters: [String: String]?
         - headers: [String: String]?
         - body: Data?
    3. Create DouyinAPIEndpoints.swift:
       - Add `import Foundation`
       - Create endpoint for getting QR code: `static func getQRCode() -> Endpoint<LoginQRCode>`
         - Base URL: https://sso.douyin.com
         - Path: /get_qr_code
       - Create endpoint for checking QR status: `static func checkQRStatus(uuid: String) -> Endpoint<QRStatusResponse>`
       - Define QRStatusResponse struct: contains confirmed: Bool, and optional access/refresh tokens
       - Use standard Douyin headers: User-Agent matching Douyin web client, accept JSON
  </action>
  <verify>
    <automated>ls C:/Users/59213/douyin/DouyinLiveTV/Data/Network/Endpoint.swift && ls C:/Users/59213/douyin/DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint.swift exists with "struct Endpoint<Response: Decodable>"
    - Endpoint defines "enum HTTPMethod"
    - DouyinAPIEndpoints.swift exists with "getQRCode()" endpoint
    - DouyinAPIEndpoints.swift exists with "checkQRStatus(uuid:)" endpoint
    - QRStatusResponse struct contains "confirmed: Bool"
  </acceptance_criteria>
  <done>Endpoint infrastructure created with Douyin QR login endpoints</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement APIClient with async/await and error handling</name>
  <files>C:/Users/59213/douyin/DouyinLiveTV/Data/Network/APIClient.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/Endpoint.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/AuthToken.swift
  </read_first>
  <action>
    Create APIClient.swift in Data/Network/:
    - Define public enum APIError: Error with cases:
      - networkError(Error)
      - invalidResponse
      - decodingError(Error)
      - unauthorized
      - forbidden
      - serverError(Int)
    - Add Equatable conformance to APIError
    - Define public class APIClient:
      - Private let urlSession: URLSession
      - Public init(urlSession: URLSession = .shared)
      - Implement public func request<T: Decodable>(_ endpoint: Endpoint<T>) async throws -> T
      - Steps: create URLRequest with method, headers, parameters, body → perform with urlSession → check status code → decode JSON → return decoded object
      - Map status codes to errors: 401 → .unauthorized, 403 → .forbidden, 500+ → .serverError
    - Use native URLSession async/await (no third-party HTTP client per research recommendation to avoid extra dependency)
  </action>
  <verify>
    <automated>ls C:/Users/59213/douyin/DouyinLiveTV/Data/Network/APIClient.swift</automated>
  </verify>
  <acceptance_criteria>
    - APIClient.swift exists with "class APIClient"
    - APIClient defines "enum APIError: Error, Equatable"
    - request method is "async throws" with generic Response type
    - 401 status code maps to .unauthorized error
    - Uses native URLSession (no Alamofire dependency)
  </acceptance_criteria>
  <done>APIClient implemented with async/await and proper error mapping</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement QR code generation using CoreImage with unit tests</name>
  <files>
  - C:/Users/59213/douyin/DouyinLiveTV/Data/Network/QRCodeGenerator.swift
  - C:/Users/59213/douyin/DouyinLiveTVTests/APIClientTests.swift
  </files>
  <behavior>
    - Test 1: generateQRCode returns non-nil UIImage for valid input string
    - Test 2: generateQRCode returns nil for empty input
  </behavior>
  <action>
    1. Create QRCodeGenerator.swift in Data/Network/:
       - Add import CoreImage and UIKit
       - Implement public function `generateQRCode(from string: String, size: CGSize) -> UIImage?
       - Use CIFilter(name: "CIQRCodeGenerator")
       - Set inputCorrectionLevel to "M"
       - Scale output image to requested size
       - Convert CIImage to UIImage
       - Follow the example pattern from 02-RESEARCH.md exactly
    2. Add unit tests to APIClientTests.swift for QR generation
  </action>
  <verify>
    <automated>xcodebuild test -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV' -only-testing:DouyinLiveTVTests/APIClientTests</automated>
  </verify>
  <acceptance_criteria>
    - QRCodeGenerator.swift exists at Data/Network/QRCodeGenerator.swift
    - Uses "CIQRCodeGenerator" CIFilter (system built-in)
    - Function returns UIImage?
    - APIClientTests.swift exists with at least one passing test for QR generation
    - All tests in APIClientTests pass
  </acceptance_criteria>
  <done>QR code generation implemented with passing tests</done>
</task>

</tasks>

<verification>
After this plan completes:
- All files exist at correct paths following project architecture
- All unit tests pass
- QR code generation produces non-nil UIImage
- API client correctly maps errors
</verification>

<success_criteria>
1. Endpoint type defined with generics for Codable responses ✓
2. Douyin QR login endpoints defined ✓
3. APIClient implemented with async/await ✓
4. Error mapping for 401/403 implemented ✓
5. QR code generation works via CoreImage ✓
6. All unit tests pass ✓
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-api-layer/02-02-SUMMARY.md`
</output>
