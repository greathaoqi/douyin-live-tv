---
phase: 05-favorites-management
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - DouyinLiveTV/UI/Favorites/FavoritesView.swift
  - DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift
autonomous: true
requirements: [FAV-02, FAV-03, FAV-04]
user_setup: []

must_haves:
  truths:
    - "User can view list of all saved favorite rooms"
    - "User can delete rooms from favorites via edit mode"
    - "User can select a room to immediately navigate to watch live"
    - "List sorts by lastViewed descending"
  artifacts:
    - path: "DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift"
      provides: "View model for favorites list with business logic"
      exports: ["FavoritesViewModel"]
    - path: "DouyinLiveTV/UI/Favorites/FavoritesView.swift"
      provides: "SwiftUI list UI with cells and edit mode delete"
      contains: "@ObservedObject var viewModel: FavoritesViewModel"
  key_links:
    - from: "FavoritesView"
      to: "FavoritesService"
      via: "FavoritesViewModel injected dependency"
    - from: "FavoritesView"
      to: "MainTabView"
      via: "selectedTab binding for programmatic navigation"
---

<objective>
Implement FavoritesView UI with list display, delete via edit mode, and selection navigation.

Per locked decisions: Plain List with full-width rows, delete via edit mode, immediate navigation on selection, sorted by lastViewed descending.

Purpose: Delivers the main favorites list UI that users interact with.
Output: Complete FavoritesView with view model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-favorites-management/05-CONTEXT.md
@.planning/phases/05-favorites-management/05-UI-SPEC.md
@.planning/phases/05-favorites-management/05-01-PLAN.md
@C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift
@C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift
@C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift

<interfaces>
From 05-01-PLAN:
- `FavoritesService` has: `@Published var favorites: [LiveRoom]`, `func fetchFavorites() async throws -> [LiveRoom]`, `func deleteRoom(_ room: LiveRoom) async throws`, `func updateLastViewed(_ room: LiveRoom) async throws`

From 05-UI-SPEC.md spacing/typography:
- xs: 4pt, sm: 8pt, md: 16pt, lg: 24pt, xl: 32pt
- Body: 20pt, Label: 24pt, Heading: 28pt, Display: 60pt

From 05-UI-SPEC.md copy:
- Empty state heading: "No Favorites Yet"
- Empty state body: "Add your first live room to start monitoring statistics from the big screen."
- Delete confirmation: "Delete Room": Are you sure you want to remove this room from favorites?
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create FavoritesViewModel</name>
  <files>DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift
  </read_first>
  <action>
Create new file `DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift`:

- Import `Foundation`, `Observation`, `SwiftData`, `DouyinLiveTVDomain`
- `@Observable class FavoritesViewModel`
- Properties:
  - `let favoritesService: FavoritesService`
  - `var favorites: [LiveRoom] = []`
  - `var showDeleteConfirmation: Bool = false`
  - `var roomToDelete: LiveRoom? = nil`
  - `var selectedRoomId: String? = nil`
- Init: `init(favoritesService: FavoritesService = DependencyContainer.shared.favoritesService)`
- Methods:
  1. `func onAppear() async` — fetch favorites from service, assign to `self.favorites`
  2. `func deleteRoom(_ room: LiveRoom) async throws` — call service delete, refresh favorites
  3. `func selectRoom(_ room: LiveRoom) async throws -> String` — update lastViewed, save as last selected, return roomId for navigation
- Observe `favoritesService.favorites` and update local `favorites` automatically

Follow the existing SwiftUI view model pattern with `@Observable`.
  </action>
  <acceptance_criteria>
    - File exists: `C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift`
    - `grep -c "@Observable class FavoritesViewModel" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift` → 1 match
    - `grep -E "(onAppear|deleteRoom|selectRoom)" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift` → all 3 methods exist
    - `grep -E "favoritesService" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift` → dependency injected
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' clean build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>FavoritesViewModel created with all business logic</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement FavoritesView with list and cells</name>
  <files>DouyinLiveTV/UI/Favorites/FavoritesView.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesViewModel.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift
    - .planning/phases/05-favorites-management/05-UI-SPEC.md
  </read_first>
  <action>
Replace the stub `FavoritesView.swift` content completely:

- Add `@ObservedObject var viewModel: FavoritesViewModel`
- Add `@Binding var selectedTab: Tab` (to allow programmatic navigation to watchLive tab)
- Add `@State private var isEditMode: Bool = false`
- Structure:
  - If `viewModel.favorites.isEmpty`: show empty state with copy from UI-SPEC (60pt heading, 28pt body) matching the stub
  - Else: `List` with each row being a full-width cell
  - Each cell (`HStack`) contains:
    - `VStack(alignment: .leading, spacing: 4pt)`: room title (24pt semibold), author nickname (20pt secondary)
    - `Spacer()`
    - `VStack(alignment: .trailing, spacing: 4pt)`:
      - Circle + Text: "Live" (green circle if live, gray if offline)
      - Text: "Last checked" + relative time (20pt secondary)
    - All interactive elements meet 88pt minimum focus size
- Enable edit mode with delete (`.deleteDisabled(!isEditMode)`)
- On row selection (tap): call `viewModel.selectRoom()` then set `selectedTab = .watchLive`
- Add toolbar button to toggle edit mode ("Edit" / "Done")
- Add delete confirmation alert that appears when `viewModel.showDeleteConfirmation` is true
- Apply spacing per 05-UI-SPEC: `lg: 24pt` between major elements, `xs: 4pt` between text lines
- Add `focusable()` and `focusEffect()` modifier to all focusable items

The view must accept `selectedTab: Binding<Tab>` from parent `MainTabView` to enable programmatic navigation.
  </action>
  <acceptance_criteria>
    - `grep -E "List|ForEach" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift` → List with ForEach exists
    - `grep -E "isEditMode|editMode" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift` → edit mode enabled
    - `grep -E "Delete Room|Are you sure" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift` → delete confirmation alert exists
    - `grep -E "@ObservedObject var viewModel" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift` → view model injected
    - `grep -E "selectedTab.*Binding" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift` → selectedTab binding exists
    - `grep -E "focusable.*focusEffect" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift` → focus effect configured
    - Empty state shows when no favorites, list shows when favorites exist
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>FavoritesView implemented with list, cells, delete, selection, navigation</done>
</task>

</tasks>

<verification>
- Empty state displays correctly when no favorites
- List displays favorites sorted by lastViewed descending
- Delete via edit mode works with confirmation
- Selection navigates to WatchLive tab
- All focus items meet 88pt minimum
- Project compiles without errors
</verification>

<success_criteria>
- User can view all saved favorite rooms in a list
- User can delete any room via edit mode
- User can select a room and be immediately navigated to watching it
- All tvOS HIG requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/05-favorites-management/05-02-SUMMARY.md`
</output>
