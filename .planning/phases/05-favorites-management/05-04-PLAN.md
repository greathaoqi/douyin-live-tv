---
phase: 05-favorites-management
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - DouyinLiveTV/UI/Main/MainTabView.swift
  - DouyinLiveTV/UI/WatchLive/WatchLiveView.swift
  - DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift
  - DouyinLiveTV/App/DouyinLiveTVApp.swift
autonomous: true
requirements: [FAV-05, FAV-06]
user_setup: []

must_haves:
  truths:
    - "MainTabView passes selectedTab binding to FavoritesView and AddRoomView"
    - "WatchLiveView can accept a room parameter and open it directly"
    - "On app launch, last viewed room is automatically opened"
    - "All UI components integrated with existing tab navigation"
  artifacts:
    - path: "DouyinLiveTV/UI/Main/MainTabView.swift"
      provides: "selectedTab made observable for programmatic navigation"
    - path: "DouyinLiveTV/App/DouyinLiveTVApp.swift"
      provides: "App launch last room recall logic"
    - path: "DouyinLiveTV/UI/WatchLive/WatchLiveView.swift"
      provides: "Accepts optional roomId parameter for direct opening"
  key_links:
    - from: "FavoritesView selection"
      to: "MainTabView"
      via: "selectedTab binding"
      pattern: "selectedTab = .watchLive"
    - from: "App entry point"
      to: "FavoritesService"
      via: "getLastSelectedRoomId" recall
    - from: "AddRoom success"
      to: "MainTabView"
      via: "selectedTab = .favorites"
---

<objective>
Wire all components together: update MainTabView to pass selectedTab binding, update WatchLiveView to accept direct room opening, implement app launch recall of last viewed room.

Per locked decisions: Last viewed room is stored in UserDefaults, automatically opened on app launch. After favorites selection/add, programmatic navigation works correctly.

Purpose: Integrate all the separately built favorites components into the working app.
Output: Fully integrated favorites management system working end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-favorites-management/05-CONTEXT.md
@C:/Users/59213/douyin/DouyinLiveTV/UI/Main/MainTabView.swift
@C:/Users/59213/douyin/DouyinLiveTV/UI/WatchLive/WatchLiveView.swift
@C:/Users/59213/douyin/DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift
@C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift
@C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift

<interfaces>
From existing MainTabView.swift:
```swift
struct MainTabView: View {
    @State private var selectedTab: Tab = .watchLive

    var body: some View {
        TabView(selection: $selectedTab) {
            WatchLiveView()
                .tabItem { ... }
                .tag(Tab.watchLive)
            FavoritesView()
                .tabItem { ... }
                .tag(Tab.favorites)
            AddRoomView()
                .tabItem { ... }
                .tag(Tab.addRoom)
        }
    }
}
```

From 05-CONTEXT.md: FAV-05 requirement: "Last viewed room is remembered and opened automatically on app launch"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update MainTabView to pass selectedTab binding to children</name>
  <files>DouyinLiveTV/UI/Main/MainTabView.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Main/MainTabView.swift
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/FavoritesView.swift
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomView.swift
  </read_first>
  <action>
Modify `MainTabView.swift`:

- For `FavoritesView()` change to `FavoritesView(selectedTab: $selectedTab)`
- For `AddRoomView()` change to `AddRoomView(selectedTab: $selectedTab)`

This enables programmatic navigation from both children to parent tab selection. Verify that `Tab` enum is accessible to the child views.
  </action>
  <acceptance_criteria>
    - `grep -E "FavoritesView\\(selectedTab:" C:/Users/59213/douyin/DouyinLiveTV/UI/Main/MainTabView.swift` → binding passed to FavoritesView
    - `grep -E "AddRoomView\\(selectedTab:" C:/Users/59213/douyin/DouyinLiveTV/UI/Main/MainTabView.swift` → binding passed to AddRoomView
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>MainTabView passes selectedTab binding to both child views</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update WatchLiveView and LiveRoomViewModel to accept optional roomId parameter</name>
  <files>DouyinLiveTV/UI/WatchLive/WatchLiveView.swift, DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/UI/WatchLive/WatchLiveView.swift
    - C:/Users/59213/douyin/DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift
  </read_first>
  <action>
Update `LiveRoomViewModel`:

- Add optional `initialRoomId: String?` parameter to init
- If `initialRoomId` is not nil, automatically fetch stats for that room on appear

Update `WatchLiveView`:

- Add `initialRoomId: String? = nil` parameter to struct
- Pass `initialRoomId` to `LiveRoomViewModel` initializer

This allows direct navigation from FavoritesView where user selects a room and goes directly to watching it.
  </action>
  <acceptance_criteria>
    - `grep -E "initialRoomId" C:/Users/59213/douyin/DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift` → initialRoomId parameter exists in init
    - `grep -E "initialRoomId: String" C:/Users/59213/douyin/DouyinLiveTV/UI/WatchLive/WatchLiveView.swift` → initialRoomId parameter exists on WatchLiveView
    - `grep -E "if.*initialRoomId != nil" C:/Users/59213/douyin/DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift` → automatic fetch when initialRoomId provided
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>WatchLiveView and view model accept initial room ID for direct opening</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement app launch last room recall in DouyinLiveTVApp</name>
  <files>DouyinLiveTV/App/DouyinLiveTVApp.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift
    - C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift
  </read_first>
  <action>
Modify `DouyinLiveTVApp`:

- Add `@State private var initialRoomId: String? = nil`
- In `init()` or `onAppear`: call `DependencyContainer.shared.favoritesService.getLastSelectedRoomId()`
- If non-nil, assign to `initialRoomId` state
- When creating `MainTabView`, pass `initialRoomId` to `WatchLiveView(initialRoomId: initialRoomId)`

This implements FAV-05: "Last viewed room is remembered and opened automatically on app launch".
  </action>
  <acceptance_criteria>
    - `grep -E "getLastSelectedRoomId" C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift` → calls FavoritesService getLastSelectedRoomId
    - `grep -E "WatchLiveView\\(initialRoomId:" C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift` → passes initialRoomId
    - `grep -E "DependencyContainer\\.shared\\.favoritesService" C:/Users/59213/douyin/DouyinLiveTV/App/DouyinLiveTVApp.swift` → uses shared container
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>App launch automatically recalls and opens last viewed room</done>
</task>

</tasks>

<verification>
- MainTabView passes selectedTab binding to both children for programmatic navigation
- WatchLiveView accepts initialRoomId for direct opening
- App launch recalls last selected room from UserDefaults
- Full build succeeds with no errors
- All requirements FAV-01 through FAV-06 are implemented
</verification>

<success_criteria>
- Programmatic navigation works from FavoritesView to WatchLiveView after selection
- Programmatic navigation works from AddRoomView to FavoritesView after successful add
- Last viewed room is automatically opened on app cold launch
- Favorites persist across app restarts via SwiftData (FAV-06)
- Full end-to-end flow works: Add → List → Select → Watch → Relaunch → auto-open last
</success_criteria>

<output>
After completion, create `.planning/phases/05-favorites-management/05-04-SUMMARY.md`
</output>
