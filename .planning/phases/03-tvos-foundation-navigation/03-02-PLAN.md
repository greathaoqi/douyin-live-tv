---
phase: 03-tvos-foundation-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DouyinLiveTV/UI/Main/MainTabView.swift
  - DouyinLiveTV/UI/Main/TabItem.swift
  - DouyinLiveTV/UI/WatchLive/WatchLiveView.swift
  - DouyinLiveTV/UI/Favorites/FavoritesView.swift
  - DouyinLiveTV/UI/AddRoom/AddRoomView.swift
  - DouyinLiveTV/UI/Common/ContentView.swift
  - DouyinLiveTVTests/FocusSizeConstraintsTests.swift
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-06, UX-07, UX-08]
user_setup: []

must_haves:
  truths:
    - "Authenticated user sees 3 tabs at the top: Watch Live, Favorites, Add Room"
    - "User can swipe with Siri Remote to move focus between tabs"
    - "Clicking a tab switches to that tab's content"
    - "All focusable items meet 88x88pt minimum size"
    - "Focused items show system parallax effect"
    - "Content doesn't get cut off at screen edges"
    - "UI adapts automatically to system dark mode"
  artifacts:
    - path: "DouyinLiveTV/UI/Main/MainTabView.swift"
      provides: "Main tab navigation container with 3 tabs"
      exports: ["MainTabView"]
    - path: "DouyinLiveTV/UI/Main/TabItem.swift"
      provides: "Tab enum with 3 cases and metadata"
      exports: ["Tab"]
    - path: "DouyinLiveTV/UI/WatchLive/WatchLiveView.swift"
      provides: "Watch Live placeholder view (to be implemented in Phase 4)"
    - path: "DouyinLiveTV/UI/Favorites/FavoritesView.swift"
      provides: "Favorites placeholder view (to be implemented in Phase 5)"
    - path: "DouyinLiveTV/UI/AddRoom/AddRoomView.swift"
      provides: "Add Room placeholder view (to be implemented in Phase 5)"
    - path: "DouyinLiveTV/UI/Common/ContentView.swift"
      provides: "Routes authenticated state to MainTabView"
    - path: "DouyinLiveTVTests/FocusSizeConstraintsTests.swift"
      provides: "Unit tests verifying minimum focus size constraints"
  key_links:
    - from: "ContentView"
      to: "MainTabView"
      via: "Displays MainTabView when authenticated"
      pattern: "MainTabView"
    - from: "MainTabView"
      to: "TabView"
      via: "Uses native TabView with tabViewStyle(.tabBar)"
      pattern: "TabView.*tabViewStyle.*tabBar"
    - from: "MainTabView"
      to: "3 tabs"
      via: "WatchLiveView, FavoritesView, AddRoomView"
    - to: ".safeAreaPadding"
      via: "Applied to root view to prevent overscan cutoff"
      pattern: "\\.safeAreaPadding"
    - from: "All interactive elements"
      to: "88pt minimum"
      via: "frame(minWidth: 88, minHeight: 88)"
      pattern: "minWidth: 88.*minHeight: 88"
    - from: "All focusable elements"
      to: "focusEffect"
      via: ".focusable().focusEffect()"
      pattern: "\\.focusable.*\\.focusEffect"
---

<objective>
Create the main tab-based navigation structure with 3 tabs and proper tvOS HIG compliance.

Purpose: This establishes the primary navigation foundation for the entire app after authentication, satisfying most UX requirements for tvOS foundation (UX-01, UX-02, UX-03, UX-04, UX-06, UX-07, UX-08).
Output: MainTabView with 3 tabs, placeholder views for future phases, updated ContentView routing, and unit tests for focus sizing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tvos-foundation-navigation/03-CONTEXT.md
@.planning/phases/03-tvos-foundation-navigation/03-RESEARCH.md
@.planning/phases/03-tvos-foundation-navigation/03-UI-SPEC.md
@DouyinLiveTV/UI/Common/ContentView.swift
</context>

<interfaces>
From UI/Common/ContentView.swift:
```swift
struct ContentView: View {
    @ObservedObject private var authStateManager: AuthStateManager
    init(authStateManager: AuthStateManager = DependencyContainer.shared.authStateManager)
    var body: some View { ... }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create main navigation structure and placeholder views</name>
  <files>
    DouyinLiveTV/UI/Main/MainTabView.swift
    DouyinLiveTV/UI/Main/TabItem.swift
    DouyinLiveTV/UI/WatchLive/WatchLiveView.swift
    DouyinLiveTV/UI/Favorites/FavoritesView.swift
    DouyinLiveTV/AddRoom/AddRoomView.swift
  </files>
  <read_first>
  </read_first>
  <action>
1. Create `DouyinLiveTV/UI/Main/` directory.
2. Create `TabItem.swift` with:
   - `enum Tab: Hashable`
   - Cases: `watchLive`, `favorites`, `addRoom`
   - Each case has computed properties: `title: String`, `systemImage: String`
   - Titles per 03-UI-SPEC.md: "Watch Live", "Favorites", "Add Room"
   - Use SF Symbols images: "tv", "star.fill", "plus"
3. Create `MainTabView.swift`:
   - `struct MainTabView: View`
   - `@State private var selectedTab: Tab = .watchLive` (default to Watch Live)
   - Use `TabView(selection: $selectedTab)` with 3 tabs
   - Each tab uses `.tabItem` matching the title and image
   - Apply `.tabViewStyle(.tabBar)` for tvOS native top tab bar (per locked D-01 decision)
   - Apply `.safeAreaPadding()` to the root view (per locked decision for overscan)
   - Set background to `Color.systemBackground` for automatic dark mode adaptation
4. Create `DouyinLiveTV/UI/WatchLive/` directory with `WatchLiveView.swift`:
   - `struct WatchLiveView: View` - placeholder with text "Watch Live\nComing in Phase 4" centered
   - Add `.safeAreaPadding()` to root view
5. Create `DouyinLiveTV/UI/Favorites/` directory with `FavoritesView.swift`:
   - `struct FavoritesView: View` - placeholder showing empty state per 03-UI-SPEC.md copy:
     - Heading: "No Favorites Yet" (60px, bold)
     - Body: "Add your first live room to start monitoring statistics from the big screen." (28px, regular, secondary)
   - Add `.safeAreaPadding()` to root view
6. Create `DouyinLiveTV/UI/AddRoom/` directory with `AddRoomView.swift`:
   - `struct AddRoomView: View` - placeholder with text "Add Room\nComing in Phase 5" centered
   - Add `.safeAreaPadding()` to root view
  </action>
  <acceptance_criteria>
    - Tab.swift contains `enum Tab: Hashable` with 3 cases: watchLive, favorites, addRoom
    - Each tab case has correct title matching 03-UI-SPEC.md copy
    - MainTabView.swift contains `TabView` with `.tabViewStyle(.tabBar)`
    - MainTabView has `.safeAreaPadding()` on root view
    - MainTabView uses `Color.systemBackground` for automatic dark mode
    - All three placeholder view files exist with basic structure
    - All three placeholder views have `.safeAreaPadding()` on root view
  </acceptance_criteria>
  <verify>
    <automated>
grep -n "enum Tab: Hashable" DouyinLiveTV/UI/Main/TabItem.swift && grep -n ".tabViewStyle(.tabBar)" DouyinLiveTV/UI/Main/MainTabView.swift && grep -n ".safeAreaPadding" DouyinLiveTV/UI/Main/MainTabView.swift
    </automated>
  </verify>
  <done>
    Main tab navigation structure created with all three tabs and placeholders. All required tvOS HIG attributes applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ContentView to route authenticated state to MainTabView</name>
  <files>DouyinLiveTV/UI/Common/ContentView.swift</files>
  <read_first>DouyinLiveTV/UI/Common/ContentView.swift</read_first>
  <action>
1. In ContentView's `mainUI` private extension, replace the current placeholder VStack with `MainTabView()`.
2. Keep the existing background modifier on the root Group.
  </action>
  <acceptance_criteria>
    - ContentView.swift imports MainTabView (same module, no import needed - just references the type)
    - In the `mainUI` computed property, the body returns `MainTabView()`
    - File still compiles with no errors
  </acceptance_criteria>
  <verify>
    <automated>
grep -n "MainTabView" DouyinLiveTV/UI/Common/ContentView.swift
    </automated>
  </verify>
  <done>
    Authenticated users now see the main tab navigation instead of the placeholder.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create unit tests for minimum focus target size constraints</name>
  <files>DouyinLiveTVTests/FocusSizeConstraintsTests.swift</files>
  <read_first></read_first>
  <behavior>
    - All focusable buttons in MainTabView context have minimum 88x88pt frames
    - Test verifies that the frame constraints are applied correctly
  </behavior>
  <action>
1. Create `DouyinLiveTVTests/FocusSizeConstraintsTests.swift`.
2. Write tests that verify a rendered focusable button with the standard pattern has minimum 88 points for both width and height.
3. Test the layout constraints by rendering in a test and checking the frame properties.
  </action>
  <acceptance_criteria>
    - FocusSizeConstraintsTests.swift exists in the test target
    - Contains at least one test method that verifies 88pt minimum
    - All tests exit with success when run
  </acceptance_criteria>
  <verify>
    <automated>
cd "C:\Users\59213\douyin" && xcodebuild test -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' -only-testing:DouyinLiveTVTests/FocusSizeConstraintsTests
    </automated>
  </verify>
  <done>
    Unit tests verify minimum 88x88pt size requirement is enforced for focusable elements.
  </done>
</task>

</tasks>

<verification>
Build the project and run unit tests. Verify that the navigation structure appears correctly in simulator when authenticated.
</verification>

<success_criteria>
- 3-tab navigation exists with native tvOS tab bar style
- All root views have safeAreaPadding for overscan protection
- All interactive elements have 88pt minimum size
- Focusable items have focusEffect for system parallax
- ContentView correctly routes authenticated users to MainTabView
- All unit tests pass
- Dark mode is automatically respected via system colors
</success_criteria>

<output>
After completion, create `.planning/phases/03-tvos-foundation-navigation/03-02-SUMMARY.md`
</output>
