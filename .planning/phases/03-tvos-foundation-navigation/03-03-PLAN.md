---
phase: 03-tvos-foundation-navigation
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - DouyinLiveTV/UI/AddRoom/AddRoomView.swift
  - DouyinLiveTV/DouyinLiveTV.xcodeproj/project.pbxproj
autonomous: false
requirements: [UX-05]
user_setup: []

must_haves:
  truths:
    - "Add Room screen has text field for room ID/URL input that supports dictation"
    - "Dictation button is available on Siri Remote for text input"
    - "App builds successfully with all navigation foundations in place"
    - "All tvOS HIG requirements are met"
  artifacts:
    - path: "DouyinLiveTV/UI/AddRoom/AddRoomView.swift"
      provides: "Updated Add Room placeholder with text field for dictation testing"
    - path: "DouyinLiveTV/DouyinLiveTV.xcodeproj/project.pbxproj"
      provides: "All new files added to Xcode project"
  key_links:
    - from: "AddRoomView"
      to: "TextField"
      via: "TextField enables dictation by default on tvOS"
      pattern: "TextField"
    - to: "Xcode project"
      via: "All new UI and Common files from this phase are included in the main app target"
---

<objective>
Enable dictation support on text input fields and perform full project build verification with human verification of all requirements.

Purpose: Completes the last requirement UX-05 (dictation), ensures all new files are in the Xcode project, builds successfully, and verifies all UX requirements work correctly in simulator.
Output: Updated AddRoomView with text field, project updated, full build, human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tvos-foundation-navigation/03-CONTEXT.md
@.planning/phases/03-tvos-foundation-navigation/03-RESEARCH.md
@.planning/phases/03-tvos-foundation-navigation/03-UI-SPEC.md
@DouyinLiveTV/UI/AddRoom/AddRoomView.swift
</context>

<interfaces>
From 03-RESEARCH.md:
> "TextField enables dictation automatically on tvOS" — no extra configuration needed.
From locked decisions:
> "Dictation - Enable dictation on all text input fields (room ID entry)"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update AddRoomView to include text field with dictation support</name>
  <files>DouyinLiveTV/UI/AddRoom/AddRoomView.swift</files>
  <read_first>DouyinLiveTV/UI/AddRoom/AddRoomView.swift</read_first>
  <action>
1. Update AddRoomView from simple placeholder to include:
   - Heading: "Add Room" (60px, bold)
   - `TextField("Enter room ID or URL", text: $roomInput)` centered
   - The TextField frame must be at least 88pt in height per HIG requirement
   - Apply `.focusable()` and `.focusEffect()` to the TextField
   - Text field uses system background color that adapts to dark mode
   - Keep `.safeAreaPadding()` on root view
2. Add `@State private var roomInput: String = ""` to hold the text binding.
3. Follow spacing from 03-UI-SPEC: 32pt spacing between heading and text field.
  </action>
  <acceptance_criteria>
    - AddRoomView contains `@State private var roomInput: String = ""`
    - AddRoomView contains `TextField("Enter room ID or URL", text: $roomInput)`
    - TextField frame has `minHeight: 88`
    - TextField has `.focusable()` modifier
    - TextField has `.focusEffect()` modifier
    - Root view still has `.safeAreaPadding()`
    - No custom disable for dictation (enabled by default per tvOS)
  </acceptance_criteria>
  <verify>
    <automated>
grep -n "TextField" DouyinLiveTV/UI/AddRoom/AddRoomView.swift && grep -n "minHeight: 88" DouyinLiveTV/UI/AddRoom/AddRoomView.swift && grep -n ".focusEffect()" DouyinLiveTV/UI/AddRoom/AddRoomView.swift
    </automated>
  </verify>
  <done>
    AddRoomView now has text field with dictation enabled automatically by tvOS, satisfying UX-05. All focus requirements met.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add all new files to Xcode project and verify full build</name>
  <files>DouyinLiveTV/DouyinLiveTV.xcodeproj/project.pbxproj</files>
  <read_first></read_first>
  <action>
1. Run `xcodebuild clean` to clean any previous build artifacts.
2. Run full build of the project for tvOS simulator to verify no compile errors.
3. All new files created in this phase must be automatically added to the project if they aren't already.
  </action>
  <acceptance_criteria>
    - Full build completes with exit code 0
    - No compile errors or linker errors
    - All new source files are included in the main app target
  </acceptance_criteria>
  <verify>
    <automated>
cd "C:\Users\59213\douyin" && xcodebuild clean && xcodebuild build -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)'
    </automated>
  </verify>
  <done>
    Project builds successfully with all new navigation foundation code included.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run all unit tests created in this phase</name>
  <files></files>
  <read_first></read_first>
  <action>
1. Run both unit test classes created in this phase (AppLifecycleTests and FocusSizeConstraintsTests).
2. Verify all tests pass.
  </action>
  <acceptance_criteria>
    - All tests in both test classes pass with exit code 0
  </acceptance_criteria>
  <verify>
    <automated>
cd "C:\Users\59213\douyin" && xcodebuild test -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' -only-testing:DouyinLiveTVTests/AppLifecycleTests -only-testing:DouyinLiveTVTests/FocusSizeConstraintsTests
    </automated>
  </verify>
  <done>
    All unit tests pass, confirming implementation correctness.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of all tvOS foundation requirements</name>
  <files></files>
  <read_first></read_first>
  <action>
Automated build and unit tests complete. Pause for human to verify behavior in tvOS simulator or physical hardware.
  </action>
  <acceptance_criteria>
Human approves all requirements working correctly:
1. Focus-based navigation works with Siri Remote (swipe moves focus, click selects)
2. Three tabs visible at top: Watch Live, Favorites, Add Room
3. Focused items show parallax effect
4. Dictation available on Add Room text field
5. No content cut off at edges
6. All focus items at least 88pt
7. Dark mode changes respected automatically
8. App lifecycle transitions work correctly
  </acceptance_criteria>
  <verify>
  </verify>
  <done>
Human has verified all 9 UX requirements work correctly according to tvOS HIG.
  </done>
  <what-built>Complete tvOS foundation and main navigation with these deliverables:</what-built>
  <how-to-verify>
1. Build and run the app in tvOS Simulator (or on physical Apple TV if available):
   - `open DouyinLiveTV.xcodeproj` and click Run
2. Verify all requirements:
   - **UX-01/UX-02:** When authenticated, you can swipe to move focus between tabs, click to select - focus moves correctly
   - **UX-03:** Three tabs appear at the top: Watch Live, Favorites, Add Room
   - **UX-04:** When a tab receives focus, you see the system parallax (scale + shadow) effect
   - **UX-05:** Navigate to Add Room tab - the text field should show dictation button available on Siri Remote (simulator: Dictation → Clickable microphone on the remote)
   - **UX-06:** Check that none of the tab bar items or content are cut off at the edges
   - **UX-07:** All focusable items (tabs, text field) are at least 88pt in size
   - **UX-08:** Try changing system appearance to Light/Dark - UI updates automatically
   - **UX-09:** Send app to background and bring back - lifecycle events are observable by future phases

Expected behavior: All requirements should work as described, as they use native system APIs.
  </how-to-verify>
  <resume-signal>Type "approved" if all requirements work correctly, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
All requirements verified through automated unit tests and manual human verification in simulator/hardware.
</verification>

<success_criteria>
- Dictation enabled on text input field (UX-05 complete)
- All new files added to Xcode project
- Full clean build succeeds with no errors
- All unit tests pass
- Human verification confirms all 9 UX requirements work correctly per tvOS HIG
</success_criteria>

<output>
After completion, create `.planning/phases/03-tvos-foundation-navigation/03-03-SUMMARY.md`
</output>
