---
phase: 07-integration-gap-closure
plan: 2
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - "DouyinLiveTV/Data/Services/FavoritesService.swift"
  - "DouyinLiveTV/Data/Services/RefreshService.swift"
autonomous: true
requirements: [FAV-01, REFRESH-02]

must_haves:
  truths:
    - "Adding a new favorite room completes successfully with valid room ID"
    - "Automatic refresh updates live statuses with valid authentication"
  artifacts:
    - path: "DouyinLiveTV/Data/Services/FavoritesService.swift"
      provides: "FavoritesService that stores streamURL when adding rooms"
      contains: "room\\.streamURL = stats\\.streamURL"
    - path: "DouyinLiveTV/Data/Services/RefreshService.swift"
      provides: "RefreshService that updates streamURL during refresh"
      contains: "room\\.streamURL = stats\\.streamURL"
  key_links:
    - from: "FavoritesService.swift"
      to: "LiveRoom"
      via: "update streamURL after fetch"
      pattern: "room\\.streamURL = stats\\.streamURL"
    - from: "RefreshService.swift"
      to: "LiveRoom"
      via: "update streamURL during refresh"
      pattern: "room\\.streamURL = stats\\.streamURL"
---

<objective>
Second part of integration gap closure: Update FavoritesService and RefreshService to persist streamURL.

This plan addresses:
- FAV-01: Complete adding a new room with stream URL capture
- REFRESH-02: Automatic refresh works with authenticated requests

Purpose: Ensures stream URLs are captured when adding favorites and updated during automatic refresh.
Output: Both services updated to maintain streamURL property on LiveRoom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-gap-closure/07-CONTEXT.md
@.planning/phases/07-integration-gap-closure/07-01-SUMMARY.md

<interfaces>
From 07-01 output:
```swift
// LiveRoom now has:
var streamURL: String?

// LiveStatsService.fetchStats() now uses authenticatedRequest
// DependencyContainer provides correctly initialized instance
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FavoritesService to save streamURL when adding/updating rooms</name>
  <files>DouyinLiveTV/Data/Services/FavoritesService.swift</files>
  <read_first>
    - DouyinLiveTV/Data/Services/FavoritesService.swift
  </read_first>
  <action>
    In `addRoom(roomId:)` method:
    After fetching stats, when updating the existing room and when creating new room:
    - Add `room.streamURL = stats.streamURL` in both code paths (existing and new room)

    This fulfills LIVE-02 and FAV-01: when a room is added, the stream URL is captured and stored in the LiveRoom model for later playback.
  </action>
  <verify>
    <automated>grep -n "room.streamURL = stats.streamURL" DouyinLiveTV/Data/Services/FavoritesService.swift</automated>
  </verify>
  <acceptance_criteria>
    - FavoritesService.addRoom updates room.streamURL from stats.streamURL for existing rooms
    - FavoritesService.addRoom sets streamURL when creating new LiveRoom
    - Two matches in the file (existing case and new room case)
  </acceptance_criteria>
  <done>streamURL is now persisted to LiveRoom when added/updated via addRoom</done>
</task>

<task type="auto">
  <name>Task 2: Update RefreshService to update streamURL during refresh</name>
  <files>DouyinLiveTV/Data/Services/RefreshService.swift</files>
  <read_first>
    - DouyinLiveTV/Data/Services/RefreshService.swift
  </read_first>
  <action>
    In `refreshAllFavorites()` method, after fetching stats, add:
    - `room.streamURL = stats.streamURL` right after updating avatarUrl

    This ensures that when automatic refresh runs (REFRESH-02 requirement), it updates the stream URL if it has changed. Since LiveStatsService is already authenticated from 07-01, this request will automatically handle token refresh.
  </action>
  <verify>
    <automated>grep -n "room.streamURL = stats.streamURL" DouyinLiveTV/Data/Services/RefreshService.swift</automated>
  </verify>
  <acceptance_criteria>
    - RefreshService.refreshAllFavorites updates room.streamURL from stats.streamURL
  </acceptance_criteria>
  <done>Automatic refresh now updates streamURL when refreshing</done>
</task>

</tasks>

<verification>
After all changes are applied:
- Adding a new favorite fetches stats via authenticated LiveStatsService and stores streamURL
- Automatic refresh cycles update streamURL if it has changed
</verification>

<success_criteria>
1. FAV-01: Adding a new room by ID/URL completes successfully with streamURL capture ✓
2. REFRESH-02: Automatic refresh runs with authenticated requests and updates streamURL ✓
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-gap-closure/07-02-SUMMARY.md`
</output>
