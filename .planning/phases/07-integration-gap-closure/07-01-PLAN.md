---
phase: 07-integration-gap-closure
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "DouyinLiveTV/Domain/Models/LiveRoom.swift"
  - "DouyinLiveTV/Data/Services/LiveStatsService.swift"
  - "DouyinLiveTV/App/DependencyContainer.swift"
autonomous: true
requirements: [AUTH-04, LIVE-02]

must_haves:
  truths:
    - "All API requests for live stats include authorization header"
    - "LiveRoom model stores stream URL retrieved from API"
  artifacts:
    - path: "DouyinLiveTV/Domain/Models/LiveRoom.swift"
      provides: "LiveRoom model with streamURL property"
      contains: "var streamURL: String\\?"
    - path: "DouyinLiveTV/Data/Services/LiveStatsService.swift"
      provides: "LiveStatsService with AuthService dependency for authenticated requests"
      min_lines: 25
      exports: ["init(apiClient:authService:)"]
  key_links:
    - from: "LiveStatsService.swift"
      to: "AuthService"
      via: "authenticatedRequest method"
      pattern: "authService\\.authenticatedRequest"
---

<objective>
First part of integration gap closure: Add streamURL to LiveRoom model and update LiveStatsService with authentication.

This plan addresses:
- AUTH-04: Token refresh handled automatically for live stats requests
- LIVE-02: Store stream URL in LiveRoom model for AVPlayer playback

Purpose: Establishes the model and service changes needed for all subsequent stream URL functionality.
Output: Updated LiveRoom model and authenticated LiveStatsService with DI wired correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-integration-gap-closure/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add streamURL property to LiveRoom model</name>
  <files>DouyinLiveTV/Domain/Models/LiveRoom.swift</files>
  <read_first>
    - DouyinLiveTV/Domain/Models/LiveRoom.swift (current state)
  </read_first>
  <action>
    Add a `streamURL` property to the `LiveRoom` model:
    - Add `var streamURL: String?` after `avatarUrl` property (makes it optional per decision in 07-CONTEXT.md)
    - Add `streamURL: String? = nil` parameter to the `init` method
    - Assign `self.streamURL = streamURL` in the init body

    The change is backwards compatible with SwiftData - no migration needed for this phase.
  </action>
  <verify>
    <automated>grep -n "var streamURL" DouyinLiveTV/Domain/Models/LiveRoom.swift</automated>
  </verify>
  <acceptance_criteria>
    - DouyinLiveTV/Domain/Models/LiveRoom.swift contains "var streamURL: String?"
    - LiveRoom init accepts "streamURL: String? = nil" parameter
    - LiveRoom init assigns "self.streamURL = streamURL"
  </acceptance_criteria>
  <done>streamURL property added to LiveRoom model with optional type</done>
</task>

<task type="auto">
  <name>Task 2: Update LiveStatsService to use AuthService for authenticated requests</name>
  <files>DouyinLiveTV/Data/Services/LiveStatsService.swift</files>
  <read_first>
    - DouyinLiveTV/Data/Services/LiveStatsService.swift (current)
    - DouyinLiveTV/Data/Network/AuthService.swift (reference for authenticatedRequest)
  </read_first>
  <action>
    Update LiveStatsService per decisions from 07-CONTEXT.md:
    1. Add `private let authService: AuthService` property
    2. Update `init` to accept `apiClient: APIClient, authService: AuthService` and store authService
    3. Change `fetchStats` method from using `apiClient.request` to `authService.authenticatedRequest` - this automatically handles token refresh (AUTH-04 requirement)

    Per the decision: Use `authService.authenticatedRequest(_:)` which already exists and handles automatic token refresh. Don't change the existing method signature - it still returns `LiveStats`, just fetch it via authenticated request now.
  </action>
  <verify>
    <automated>grep -n "authService.authenticatedRequest" DouyinLiveTV/Data/Services/LiveStatsService.swift</automated>
  </verify>
  <acceptance_criteria>
    - LiveStatsService has `private let authService: AuthService` property
    - LiveStatsService.init accepts `authService: AuthService` parameter
    - fetchStats method uses `try await authService.authenticatedRequest(endpoint)`
    - Compiles with existing method return type
  </acceptance_criteria>
  <done>LiveStatsService now uses authenticated requests with automatic token refresh</done>
</task>

<task type="auto">
  <name>Task 3: Update dependency injection in DependencyContainer</name>
  <files>DouyinLiveTV/App/DependencyContainer.swift</files>
  <read_first>
    - DouyinLiveTV/App/DependencyContainer.swift
    - DouyinLiveTV/Data/Services/FavoritesService.swift
    - DouyinLiveTV/Data/Services/RefreshService.swift
    - DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift
  </read_first>
  <action>
    Update the LiveStatsService initialization to match the new signature:
    1. In DependencyContainer: Change LiveStatsService init from `LiveStatsService(apiClient: self.apiClient)` to `LiveStatsService(apiClient: self.apiClient, authService: self.authService)`
    2. No changes needed to FavoritesService.init signature - it still just takes LiveStatsService as a client, doesn't need to know about the auth dependency
    3. No changes needed to RefreshService.init or LiveRoomViewModel - same pattern, they just store the client instance already provided by container

    All other services keep their existing signatures - only the top-level container needs to pass the new dependency.
  </action>
  <verify>
    <automated>grep -n "LiveStatsService(apiClient:" DouyinLiveTV/App/DependencyContainer.swift | grep -w authService</automated>
  </verify>
  <acceptance_criteria>
    - DependencyContainer initializes LiveStatsService with both apiClient and authService parameters
    - No compile errors due to missing parameter
  </acceptance_criteria>
  <done>Dependency injection updated - all uses of LiveStatsService now get the authenticated version</done>
</task>

</tasks>

<verification>
After all changes are applied:
- LiveRoom model compiles with new streamURL property
- LiveStatsService compiles with AuthService dependency
- DependencyContainer injects the correct parameters
</verification>

<success_criteria>
1. AUTH-04: LiveStats requests automatically handle token refresh when expired ✓
2. LIVE-02: LiveRoom stores streamURL that can be used by AVPlayer for playback ✓
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-gap-closure/07-01-SUMMARY.md`
</output>
