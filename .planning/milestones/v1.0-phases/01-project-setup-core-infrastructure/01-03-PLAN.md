---
phase: 01-project-setup-core-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Full clean build completes with no errors"
    - "All unit tests run and pass"
    - "All folder structure verifications pass"
    - "All success criteria from phase goal are met"
  artifacts:
    - All artifacts from previous plans exist and are correct
  key_links:
    - Final build verifies all previous work
    - Tests passing confirms model code is correct
---

<objective>
Perform full clean build and test validation to confirm all phase success criteria are met. Verify project setup is complete and correct before proceeding to next phase.

Purpose: Catch any configuration issues early before they block subsequent development. Ensures the foundation is solid.
Output: Verified successful build and test run, phase validation complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-core-infrastructure/01-RESEARCH.md
@.planning/phases/01-project-setup-core-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full clean build verification</name>
  <read_first>
  Files to read before starting:
  - DouyinLiveTV.xcodeproj/project.pbxproj (verify project exists)
  - .planning/phases/01-project-setup-core-infrastructure/01-01-SUMMARY.md (verify previous work)
  - .planning/phases/01-project-setup-core-infrastructure/01-02-SUMMARY.md (verify previous work)
  </read_first>
  <files>(No file changes — verification only)</files>
  <action>
Run a full clean build with the simulator destination to verify everything compiles successfully:

```bash
xcodebuild clean build -scheme DouyinLiveTV -destination "platform=tvOS Simulator,name=Apple TV 4K (3rd generation)"
```

The build must complete with exit code 0 and no errors. If there are errors, diagnose and fix them before proceeding. Common issues to check:
- Dependencies not resolved correctly → re-resolve SPM packages
- SwiftData import errors → verify deployment target is 17.0+
- Missing files → verify folder structure matches plan
</action>
  <acceptance_criteria>
  - Command exits with code 0
  - Output contains "BUILD SUCCEEDED"
  - No errors reported in build output
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild clean build -scheme DouyinLiveTV -destination "platform=tvOS Simulator,name=Apple TV 4K (3rd generation)" | grep -E "(BUILD SUCCEEDED)"</automated>
  </verify>
  <done>Full clean build completes with BUILD SUCCEEDED, no errors</done>
</task>

<task type="auto">
  <name>Task 2: Run unit tests</name>
  <read_first>
  Files to read before starting:
  - DouyinLiveTVTests/ModelTests.swift (verify tests exist)
  - Previous build output from Task 1 (must succeed before testing)
  </read_first>
  <files>(No file changes — verification only)</files>
  <action>
Run all unit tests to verify they all pass:

```bash
xcodebuild test -scheme DouyinLiveTV -destination "platform=tvOS Simulator,name=Apple TV 4K (3rd generation)"
```

All tests must pass. If any test fails, diagnose the issue with the model implementation and fix it.
</action>
  <acceptance_criteria>
  - Command exits with code 0
  - Output contains "TEST SUCCEEDED"
  - All tests reported as passed, 0 tests failed
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme DouyinLiveTV -destination "platform=tvOS Simulator,name=Apple TV 4K (3rd generation)" | grep -E "(TEST SUCCEEDED)"</automated>
  </verify>
  <done>All unit tests pass successfully</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Manual configuration verification</name>
  <read_first>
  Files to read before starting:
  - .planning/ROADMAP.md (phase success criteria)
  - Build output from Task 1 (automated build result)
  - Test output from Task 2 (automated test result)
  </read_first>
  <files>(No file changes — verification only)</files>
  <action>
Automated build and tests have passed. User must manually verify the Xcode project configuration matches requirements.
  </action>
  <acceptance_criteria>
  1. Minimum deployment target is tvOS 17.0 or higher (verified in Xcode project settings)
  2. All four root groups (App, UI, Domain, Data) exist in Project Navigator
  3. LiveRoom.swift and LiveStats.swift exist under Domain/Models
  4. ModelTests.swift exists under DouyinLiveTVTests
  5. Alamofire and KeychainSwift are listed in Package Dependencies
  6. Product → Build succeeds in Xcode
  7. Product → Test all passes in Xcode
  </acceptance_criteria>
  <verify>
  </verify>
  <done>User has verified configuration is correct</done>
  <what-built>Complete project setup with Xcode project, four-layer folder structure, SPM dependencies, and core domain models.</what-built>
  <how-to-verify>
Manual verification steps:

1. **Open project in Xcode:**
   - Open `DouyinLiveTV.xcodeproj`
   - Go to project settings → "DouyinLiveTV" target → "General" tab
   - Verify "Minimum Deployments" → tvOS is 17.0 or higher

2. **Verify folder structure in Project Navigator:**
   - Check that `App/`, `UI/`, `Domain/`, `Data/` groups exist at root
   - Under Domain → Models: verify `LiveRoom.swift` and `LiveStats.swift` exist
   - Under `DouyinLiveTVTests`: verify `ModelTests.swift` exists

3. **Verify Swift Package dependencies:**
   - Go to project → "Package Dependencies" tab
   - Verify Alamofire (5.9.0+) and KeychainSwift (20.0.0+) are listed

4. **Build and run in simulator:**
   - Product → Build (should succeed)
   - Product → Test (all tests should pass)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
All verifications must pass:
1. Clean build → success
2. Unit tests → all pass
3. Manual verification → user approves configuration
</verification>

<success_criteria>
All phase success criteria are satisfied:
1. Xcode project created with correct tvOS 17+ target ✓
2. Folder structure established for four layers (App, UI, Domain, Data) ✓
3. Third-party dependencies added via Swift Package Manager (Alamofire, KeychainSwift) ✓
4. Project builds successfully with no errors ✓
5. Core domain models defined (LiveRoom, LiveStats) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-core-infrastructure/01-03-SUMMARY.md`
</output>
