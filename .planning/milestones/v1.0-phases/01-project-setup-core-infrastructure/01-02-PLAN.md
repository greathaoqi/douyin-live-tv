---
phase: 01-project-setup-core-infrastructure
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified: [DouyinLiveTV/Domain/Models/LiveRoom.swift, DouyinLiveTV/Domain/Models/LiveStats.swift, DouyinLiveTVTests/ModelTests.swift]
autonomous: true
requirements: []

must_haves:
  truths:
    - "LiveRoom model is defined as SwiftData @Model for persistence"
    - "LiveStats model is defined as Codable for API parsing"
    - "All model properties match expected Douyin API structure"
    - "Unit tests verify Codable conformance works correctly"
    - "Project still builds successfully after adding models"
  artifacts:
    - path: "DouyinLiveTV/Domain/Models/LiveRoom.swift"
      provides: "LiveRoom SwiftData model for favorite rooms"
      contains: "@Model class LiveRoom"
    - path: "DouyinLiveTV/Domain/Models/LiveStats.swift"
      provides: "LiveStats Codable struct for live statistics"
      contains: "struct LiveStats: Codable, Equatable"
    - path: "DouyinLiveTVTests/ModelTests.swift"
      provides: "Unit tests for model encoding/decoding"
  key_links:
    - from: "LiveRoom.swift"
      to: "SwiftData"
      via: "@Model attribute import"
      pattern: "import SwiftData"
    - from: "LiveStats.swift"
      to: "Foundation"
      via: "Codable conformance"
      pattern: "Codable"
    - from: "ModelTests.swift"
      to: "XCTest"
      via: "Unit testing framework"
---

<objective>
Define the core domain models (LiveRoom and LiveStats) and create unit tests to verify Codable conformance works correctly.

Purpose: Core models are used throughout the app — defining them early establishes the data contract for all subsequent layers. Unit tests ensure JSON parsing will work correctly with the Douyin API.
Output: Working Swift models with unit tests that validate encoding/decoding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-core-infrastructure/01-RESEARCH.md

<interfaces>
From 01-RESEARCH.md, the model structures are predefined:

**LiveRoom (SwiftData):**
```swift
@Model
class LiveRoom {
    @Attribute(.unique) var roomId: String
    var title: String
    var nickname: String
    var avatarUrl: String?
    var isLive: Bool
    var lastChecked: Date
    var lastViewed: Date?
}
```

**LiveStats (Codable):**
```swift
struct LiveStats: Codable, Equatable {
    let viewerCount: Int
    let likeCount: Int
    let totalGiftValue: Int
    let isLive: Bool
    let startTime: Date?
}
```
</interfaces>
</context>

<feature>
  <read_first>
  Files to read before starting:
  - .planning/phases/01-project-setup-core-infrastructure/01-RESEARCH.md (for exact model structure)
  - DouyinLiveTV/Domain/Models/ (directory to check)
  - DouyinLiveTVTests/ (test directory to check)
  </read_first>
  <name>Core Domain Models</name>
  <files>DouyinLiveTV/Domain/Models/LiveRoom.swift, DouyinLiveTV/Domain/Models/LiveStats.swift, DouyinLiveTVTests/ModelTests.swift</files>
  <behavior>
Create tests that verify:
- Test 1: LiveStats can be decoded from sample JSON with all fields present
- Test 2: LiveStats decodes correctly with optional startTime nil
- Test 3: LiveRoom can be instantiated with all required properties
- Test 4: Model properties match the expected naming from Douyin API

Expected: All tests pass after implementation.
  </behavior>
  <acceptance_criteria>
  - `grep "@Model" DouyinLiveTV/Domain/Models/LiveRoom.swift` → matches (SwiftData model attribute)
  - `grep "import SwiftData" DouyinLiveTV/Domain/Models/LiveRoom.swift` → matches (SwiftData imported)
  - `grep "@Attribute(.unique) var roomId" DouyinLiveTV/Domain/Models/LiveRoom.swift` → matches (unique constraint on roomId)
  - `grep "struct LiveStats: Codable, Equatable" DouyinLiveTV/Domain/Models/LiveStats.swift` → matches (correct conformances)
  - `grep "viewerCount" DouyinLiveTV/Domain/Models/LiveStats.swift` → matches (viewerCount property exists)
  - `grep "likeCount" DouyinLiveTV/Domain/Models/LiveStats.swift` → matches (likeCount property exists)
  - `grep "totalGiftValue" DouyinLiveTV/Domain/Models/LiveStats.swift` → matches (totalGiftValue property exists)
  - `test -f DouyinLiveTVTests/ModelTests.swift` → test file exists
  - `grep "import XCTest" DouyinLiveTVTests/ModelTests.swift` → XCTest imported
  - `grep "testLiveStatsDecoding" DouyinLiveTVTests/ModelTests.swift` → decoding test exists
  - `grep "Alamofire" DouyinLiveTV/Domain/Models/*.swift` → no matches (Domain doesn't depend on external libraries)
  </acceptance_criteria>
  <implementation>
Implement the core models exactly as defined in the research:

1. **LiveRoom.swift** - Create SwiftData model with `@Model` attribute, `@Attribute(.unique)` on `roomId`, all properties as defined, complete initializer.

2. **LiveStats.swift** - Create struct conforming to `Codable` and `Equatable`, all properties `let` constants, complete initializer.

3. Both models go in `DouyinLiveTV/Domain/Models/`. Domain layer has no external dependencies beyond Foundation and SwiftData (which is system framework). Do NOT add Alamofire imports here.

Follow the SwiftData model conventions exactly — `@Model` is a macro that generates the required persistence code.
  </implementation>
</feature>

<verification>
Run full test suite to verify all tests pass. Project build must still succeed.
</verification>

<success_criteria>
1. Core domain models LiveRoom and LiveStats are defined ✓
2. LiveRoom uses SwiftData @Model for persistence ✓
3. LiveStats conforms to Codable for API parsing ✓
4. All unit tests pass ✓
5. Project builds successfully with no errors ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-core-infrastructure/01-02-SUMMARY.md`
</output>
