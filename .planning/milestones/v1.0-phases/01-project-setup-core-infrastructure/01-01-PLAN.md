---
phase: 01-project-setup-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [DouyinLiveTV.xcodeproj/project.pbxproj, DouyinLiveTV/DouyinLiveTVApp.swift, DouyinLiveTV/App/, DouyinLiveTV/UI/, DouyinLiveTV/Domain/, DouyinLiveTV/Data/]
autonomous: false
requirements: []

must_haves:
  truths:
    - "Xcode project exists with tvOS 17+ target"
    - "Four-layer folder structure (App, UI, Domain, Data) is established"
    - "Third-party dependencies (Alamofire, KeychainSwift) are added via Swift Package Manager"
    - "Core domain models (LiveRoom, LiveStats) are defined"
    - "Project builds successfully with no errors"
  artifacts:
    - path: "DouyinLiveTV.xcodeproj"
      provides: "Xcode project with tvOS target"
      min_lines: 0
    - path: "DouyinLiveTV/App/"
      provides: "App layer (entry point, configuration, DI container)"
    - path: "DouyinLiveTV/UI/"
      provides: "UI layer (Views, ViewModels by screen)"
    - path: "DouyinLiveTV/Domain/Models/"
      provides: "Domain layer with core models"
    - path: "DouyinLiveTV/Data/"
      provides: "Data layer (API, authentication, local storage)"
    - path: "DouyinLiveTV/Domain/Models/LiveRoom.swift"
      provides: "LiveRoom SwiftData model"
      contains: "@Model class LiveRoom"
    - path: "DouyinLiveTV/Domain/Models/LiveStats.swift"
      provides: "LiveStats Codable struct"
      contains: "struct LiveStats: Codable"
    - path: "DouyinLiveTVTests/ModelTests.swift"
      provides: "Unit tests for core models"
  key_links:
    - from: "DouyinLiveTV.xcodeproj"
      to: "Alamofire, KeychainSwift"
      via: "Swift Package Manager dependencies"
    - from: "DouyinLiveTV/Domain/Models"
      to: "Foundation, SwiftData"
      via: "Module imports"
    - from: "DouyinLiveTVTests/ModelTests.swift"
      to: "LiveRoom, LiveStats"
      via: "Test imports for Codable validation"
---

<objective>
Create the initial Xcode project with correct tvOS 17+ target and establish the four-layer clean architecture folder structure.

Purpose: This is the foundational step for the entire project — without a properly configured project and folder structure, no further development can proceed.
Output: Xcode project configured for tvOS 17+, empty folder structure for four layers, test target scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-core-infrastructure/01-RESEARCH.md
@.planning/phases/01-project-setup-core-infrastructure/01-VALIDATION.md

<interfaces>
From 01-RESEARCH.md, the expected folder structure:
```
DouyinLiveTV/
├── App/                # App entry point, configuration, DI container
├── UI/                 # Views, ViewModels by screen
│   ├── Common/         # Shared UI components
│   ├── RoomList/
│   ├── LiveRoom/
│   └── AddRoom/
├── Domain/             # Business logic, models, use cases, repository protocols
│   ├── Models/
│   ├── UseCases/
│   └── Repositories/
└── Data/               # Implementations, API client, authentication, local storage
    ├── API/
    ├── Authentication/
    ├── Local/
    └── Repositories/
```

Core model structures are defined in research:
- `LiveRoom` - SwiftData `@Model` for persistent favorite storage
- `LiveStats` - Codable struct for API response parsing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with tvOS 17+ target</name>
  <read_first>
  Files to read before starting:
  - .planning/phases/01-project-setup-core-infrastructure/01-RESEARCH.md (for project parameters and deployment target requirements)
  - DouyinLiveTV.xcodeproj/project.pbxproj (if it exists already)
  - DouyinLiveTV/DouyinLiveTVApp.swift (if it exists already)
  </read_first>
  <files>DouyinLiveTV.xcodeproj/project.pbxproj, DouyinLiveTV/DouyinLiveTVApp.swift</files>
  <action>
Create a new Xcode project at `DouyinLiveTV/` with these parameters:
- Template: tvOS > App
- Interface: SwiftUI
- Language: Swift
- Product Name: DouyinLiveTV
- Team: None (user will configure later)
- Organization Identifier: com.douyinlivetv (user can change later)
- Minimum Deployments: tvOS 17.0 (required for SwiftData)

Use `xcodebuild` to create the project programmatically. After creation:
- Verify deployment target is set to 17.0 in project.pbxproj
- Remove any default files that don't fit the four-layer structure (move the entry point to App layer)
- Create the test files scaffold as specified in 01-VALIDATION.md

Important: SwiftData requires tvOS 17+, do NOT accept a lower default.
</action>
  <acceptance_criteria>
  - `grep "IPHONEOS_DEPLOYMENT_TARGET = 17.0" DouyinLiveTV.xcodeproj/project.pbxproj` finds deployment target setting
  - `test -f DouyinLiveTV/App/DouyinLiveTVApp.swift` → file exists in correct layer
  - `test -d DouyinLiveTVTests` → test directory exists
  - Project file `DouyinLiveTV.xcodeproj/project.pbxproj` exists and is non-empty
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild clean -scheme DouyinLiveTV -destination "platform=tvOS Simulator,name=Apple TV 4K (3rd generation)"</automated>
  </verify>
  <done>Xcode project created, deployment target set to tvOS 17.0, project file written to disk</done>
</task>

<task type="auto">
  <name>Task 2: Create four-layer architecture folder structure</name>
  <read_first>
  Files to read before starting:
  - .planning/phases/01-project-setup-core-infrastructure/01-RESEARCH.md (for folder structure specification)
  - DouyinLiveTV.xcodeproj/project.pbxproj (to update group structure)
  </read_first>
  <files>DouyinLiveTV/App/, DouyinLiveTV/UI/, DouyinLiveTV/Domain/, DouyinLiveTV/Data/</files>
  <action>
Create the complete folder structure as defined in the research:
- `DouyinLiveTV/App/` — move `DouyinLiveTVApp.swift` here, add `DependencyContainer.swift` empty scaffold
- `DouyinLiveTV/UI/` — create subdirectories: `Common/`, `RoomList/`, `LiveRoom/`, `AddRoom/`
- `DouyinLiveTV/Domain/` — create subdirectories: `Models/`, `UseCases/`, `Repositories/`
- `DouyinLiveTV/Data/` — create subdirectories: `API/`, `Authentication/`, `Local/`, `Repositories/`
- `DouyinLiveTV/Preview Content/` — keep Xcode default
- `DouyinLiveTV/Assets.xcassets` — keep Xcode default

Add a README.md in each directory briefly describing the layer's purpose. Follow the layering rules strictly:
- Domain has no external dependencies
- UI depends only on Domain
- Data implements Domain protocols
</action>
  <acceptance_criteria>
  - `test -d DouyinLiveTV/App` → App layer exists
  - `test -d DouyinLiveTV/UI/Common` → UI subdirectories exist
  - `test -d DouyinLiveTV/UI/RoomList && test -d DouyinLiveTV/UI/LiveRoom && test -d DouyinLiveTV/UI/AddRoom` → all UI subdirs exist
  - `test -d DouyinLiveTV/Domain/Models && test -d DouyinLiveTV/Domain/UseCases && test -d DouyinLiveTV/Domain/Repositories` → all Domain subdirs exist
  - `test -d DouyinLiveTV/Data/API && test -d DouyinLiveTV/Data/Authentication && test -d DouyinLiveTV/Data/Local && test -d DouyinLiveTV/Data/Repositories` → all Data subdirs exist
  - `test -f DouyinLiveTV/App/README.md && test -f DouyinLiveTV/UI/README.md && test -f DouyinLiveTV/Domain/README.md && test -f DouyinLiveTV/Data/README.md` → each layer has README
  - `grep -l "layer's purpose" DouyinLiveTV/**/README.md | wc -l | grep "4"` → 4 README files found
  </acceptance_criteria>
  <verify>
    <automated>find DouyinLiveTV -type d | sort</automated>
  </verify>
  <done>All four layers and subdirectories exist with README files describing each layer's purpose</done>
</task>

<task type="auto">
  <name>Task 3: Add core dependencies via Swift Package Manager</name>
  <read_first>
  Files to read before starting:
  - .planning/phases/01-project-setup-core-infrastructure/01-RESEARCH.md (for dependency URLs and versions)
  - DouyinLiveTV.xcodeproj/project.pbxproj (to modify dependencies)
  </read_first>
  <files>DouyinLiveTV.xcodeproj/project.pbxproj, DouyinLiveTV.xcworkspace/</files>
  <action>
Add these core dependencies to the Xcode project via Swift Package Manager:
- Alamofire: https://github.com/Alamofire/Alamofire.git from: 5.9.0 (networking)
- KeychainSwift: https://github.com/evgenyneu/keychain-swift.git from: 20.0.0 (secure token storage)

Kingfisher and SwiftJWT are deferred to later phases if needed — do NOT add them now (per pitfall avoidance guidance).

Verify both dependencies are added correctly to the project file, linked to the main target.
</action>
  <acceptance_criteria>
  - `grep -A 3 "Alamofire" DouyinLiveTV.xcodeproj/project.pbxproj` → Alamofire dependency found
  - `grep -A 3 "keychain-swift" DouyinLiveTV.xcodeproj/project.pbxproj` → KeychainSwift dependency found
  - `grep -E "(5\.9\.0|20\.0\.0)" DouyinLiveTV.xcodeproj/project.pbxproj` → correct versions found
  - `grep "Kingfisher" DouyinLiveTV.xcodeproj/project.pbxproj` → no match (Kingfisher deferred)
  - `grep "SwiftJWT" DouyinLiveTV.xcodeproj/project.pbxproj` → no match (SwiftJWT deferred)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -resolvePackageDependencies -scheme DouyinLiveTV</automated>
  </verify>
  <done>Alamofire and KeychainSwift added via SPM, dependencies resolved successfully</done>
</task>

</tasks>

<verification>
After completing all Wave 1 tasks, run:
1. `xcodebuild clean build -scheme DouyinLiveTV -destination "platform=tvOS Simulator,name=Apple TV 4K (3rd generation)"` — build must succeed with no errors
2. Verify folder structure matches the four-layer architecture defined in research
3. Verify minimum deployment target is 17.0 or higher
</verification>

<success_criteria>
1. Xcode project created with correct tvOS 17+ target ✓
2. Four-layer folder structure (App, UI, Domain, Data) established ✓
3. Core dependencies (Alamofire, KeychainSwift) added via Swift Package Manager ✓
4. Project resolves dependencies and builds successfully ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-core-infrastructure/01-01-SUMMARY.md`
</output>
