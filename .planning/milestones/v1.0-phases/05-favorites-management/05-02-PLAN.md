---
phase: 05-favorites-management
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - DouyinLiveTV/UI/Favorites/FavoritesView.swift
  - DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift
  - DouyinLiveTVTests/DouyinLiveTVTests/FavoritesViewTests.swift
autonomous: true
requirements: [FAV-02, FAV-03, FAV-04]
user_setup: []

must_haves:
  truths:
    - "User can view a list of all saved favorite rooms"
    - "User can delete rooms from favorites via edit mode"
    - "User can quickly select a favorite room to monitor"
    - "List is sorted by lastViewed descending (most recent at top)"
  artifacts:
    - path: "DouyinLiveTV/UI/Favorites/FavoritesView.swift"
      provides: "Main favorites list UI with edit mode delete"
      min_lines: 50
    - path: "DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift"
      provides: "Individual cell rendering with room title, author, live indicator"
      min_lines: 40
  key_links:
    - from: "DouyinLiveTV/UI/Favorites/FavoritesView.swift"
      to: "SwiftData @Query"
      via: "automatic sorted fetch of LiveRoom"
      pattern: "@Query.*LiveRoom"
    - from: "DouyinLiveTV/UI/Favorites/FavoritesView.swift"
      to: "selectedTab"
      via: "immediate navigation on selection to .watchLive"
      pattern: "selectedTab = .watchLive"
    - from: "DouyinLiveTV/UI/Favorites/FavoritesView.swift"
      to: "modelContext"
      via: "onDelete in edit mode"
      pattern: "onDelete"
---

<objective>
Implement FavoritesView with list of favorite rooms, edit-mode delete, and selection navigation.

This plan completes FAV-02 (view list), FAV-03 (delete rooms), and FAV-04 (select to monitor).
Uses SwiftData @Query for automatic fetching per locked decision (no manual view model needed).
Output: Working favorites list UI that matches tvOS HIG and user decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-favorites-management/05-CONTEXT.md
@.planning/phases/05-favorites-management/05-RESEARCH.md
@.planning/phases/05-favorites-management/05-UI-SPEC.md
@C:\Users\59213\douyin\DouyinLiveTV\Data\Services\FavoritesService.swift
@C:\Users\59213\douyin\DouyinLiveTV\Domain\Models\LiveRoom.swift
@C:\Users\59213\douyin\DouyinLiveTV\App\DependencyContainer.swift

<interfaces>
From LiveRoom.swift:
```swift
@Model
class LiveRoom {
    @Attribute(.unique) var roomId: String
    var title: String
    var nickname: String
    var avatarUrl: String?
    var isLive: Bool
    var lastChecked: Date
    var lastViewed: Date?
}
```

From FavoritesService.swift:
```swift
public class FavoritesService {
    public func fetchFavorites() async throws -> [LiveRoom]
    public func deleteRoom(_ room: LiveRoom) async throws
    public func updateLastViewed(_ room: LiveRoom) async throws
    public func saveLastSelectedRoomId(_ roomId: String)
}
```

From MainTabView:
```swift
struct FavoritesView: View {
    @Binding var selectedTab: Tab
}

enum Tab {
    case watchLive, favorites, addRoom
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FavoriteRoomCell view</name>
  <files>DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift</files>
  <read_first>
    - C:\Users\59213\douyin\DouyinLiveTV\Domain\Models\LiveRoom.swift
    - C:\Users\59213\douyin\.planning\phases\05-favorites-management\05-UI-SPEC.md
    - C:\Users\59213\douyin\.planning\phases\05-favorites-management\05-CONTEXT.md
  </read_first>
  <action>
Create FavoriteRoomCell.swift in UI/Favorites/ that renders a single favorite room row.

Cell content requirements (per locked decision from 05-CONTEXT.md):
- Show room title (24pt semibold, .primary color)
- Show author nickname (20pt, .secondary color)
- Show colored live indicator (green circle fill for live, gray for offline) with "Live"/"Offline" text
- Show last checked date/time (16pt, .tertiary color) formatted relative to now
- Minimum height 88pt to meet tvOS HIG focus requirement
- Full width cell with padding 16pt horizontal
- HStack with VStack on leading for title/nickname, VStack on trailing for indicator/last checked
- Apply .focusable() and .focusEffect() modifiers

Follow the spacing scale from 05-UI-SPEC.md:
- Vertical spacing: 4pt between text lines (xs)
- Horizontal spacing: 16pt between leading and trailing sections (md)
- Outer padding: 16pt horizontal, 8pt vertical (sm)

Use native system colors that automatically respect dark mode:
- Live indicator: .green
- Offline indicator: .secondary
- Title: .primary
- Nickname: .secondary
- Last checked: .tertiary
  </action>
  <acceptance_criteria>
    - File exists at DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift
    - grep -c "struct FavoriteRoomCell: View" DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift → 1 match
    - grep -c "(LiveRoom" DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift → 1 match (takes LiveRoom parameter)
    - grep -c "focusable.*focusEffect" DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift → at least 1 match
    - File contains 40+ lines of code → all information displayed
  </acceptance_criteria>
  <verify>
    <automated>ls C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\ | grep FavoriteRoomCell</automated>
  </verify>
  <done>
- File exists with correct structure
- Type `FavoriteRoomCell` is defined that takes a `LiveRoom` parameter
- All 4 pieces of information (title, nickname, live indicator, last checked) are displayed
- All interactive elements meet 88pt minimum height
- `.focusable()` and `.focusEffect()` modifiers are applied
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement FavoritesView with list, edit mode, and selection</name>
  <files>DouyinLiveTV/UI/Favorites/FavoritesView.swift</files>
  <read_first>
    - C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\FavoritesView.swift
    - C:\Users\59213\douyin\DouyinLiveTV\Data\Services\FavoritesService.swift
    - C:\Users\59213\douyin\.planning\phases\05-favorites-management\05-CONTEXT.md
    - C:\Users\59213\douyin\.planning\phases\05-favorites-management\05-UI-SPEC.md
  </read_first>
  <action>
Replace the stub implementation of FavoritesView completely:

1. Import SwiftData and add property wrappers:
   - `@Query(sort: \LiveRoom.lastViewed, order: .reverse) private var rooms: [LiveRoom]`
     - This automatically fetches all favorites sorted by lastViewed descending per decision D-04
   - `@Environment(\.modelContext) private var modelContext`
   - `@Environment(\.dismiss) private var dismiss` (not used, included by convention)
   - `@State private var isEditing = false` for edit mode state
   - Keep `@Binding var selectedTab: Tab` from existing stub

2. Structure the body:
   - If `rooms.isEmpty`: show empty state using copy from 05-UI-SPEC.md:
     - Heading: "No Favorites Yet" (60pt bold)
     - Body: "Add your first live room to start monitoring statistics from the big screen." (28pt, .secondary)
   - Else: `List` with `ForEach(rooms) { room in ... }`
     - Use `.listStyle(.plain)`
     - Use `FavoriteRoomCell(room: room)` for each row
     - Enable onDelete: `.onDelete(perform: deleteRoom)` to satisfy "delete via edit mode" decision
   - Navigation toolbar contains Edit/Done button that toggles `isEditing`

3. Implement cell selection:
   - When a cell is selected (`.onTapGesture`):
     1. Get the favorites service from `DependencyContainer.shared.favoritesService`
     2. Call `try? await favoritesService.updateLastViewed(room)` to update timestamp
     3. Call `favoritesService.saveLastSelectedRoomId(room.roomId)` to persist for app launch
     4. Set `selectedTab = .watchLive` to navigate immediately to watch live tab
     - This satisfies "immediate navigation after selection" per locked decision

4. Implement deleteRoom function:
   - `private func deleteRoom(at offsets: IndexSet) { ... }`
   - Get the room at offset, call `modelContext.delete(room)`
   - Try to save context: `try? modelContext.save()`

5. Apply environment for edit mode: `.environment(\.editMode, $isEditing)`

6. All interactive items (Edit/Done button, cells) must:
   - Meet 88pt minimum height
   - Have `.focusable()` modifier
   - Have `.focusEffect()` modifier

The implementation uses SwiftData @Query per locked decision from 05-CONTEXT.md: "SwiftData automatically fetches favorites on view appear - no manual refresh needed". No separate view model is needed.
  </action>
  <acceptance_criteria>
    - grep -n "@Query.*sort.*lastViewed.*reverse" C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\FavoritesView.swift → 1 match
    - grep -n "selectedTab.*=.watchLive" C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\FavoritesView.swift → 1 match
    - grep -n "onDelete" C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\FavoritesView.swift → 1 match
    - grep -n "editMode.*isEditing" C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\FavoritesView.swift → 1 match
    - grep -n "FavoriteRoomCell" C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\FavoritesView.swift → 1 match
    - grep -n "focusable.*focusEffect" C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\FavoritesView.swift → at least 2 matches (Edit button + cells)
    - Empty state kept for when no favorites exist
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' clean build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>
- Uses `@Query(sort: \LiveRoom.lastViewed, order: .reverse)` for automatic sorted fetching
- List displays all favorited rooms using FavoriteRoomCell
- Edit button toggles edit mode with delete buttons on each row
- Delete removes room from SwiftData and saves changes
- Cell selection updates lastViewed timestamp, saves to UserDefaults, navigates to watch live tab
- Empty state displays when no favorites exist
- All focus targets meet minimum 88pt size
  </done>
</task>

<task type="auto">
  <name>Task 3: Move existing AddRoomView to correct directory and clean up</name>
  <files>DouyinLiveTV/UI/AddRoom/AddRoomView.swift</files>
  <read_first>
    - C:\Users\59213\douyin\DouyinLiveTV\UI\Favorites\AddRoomView.swift
    - C:\Users\59213\douyin\DouyinLiveTV\UI\AddRoom\AddRoomView.swift
    - C:\Users\59213\douyin\DouyinLiveTV\UI\Main\MainTabView.swift
  </read_first>
  <action>
There are two AddRoomView files. The complete implementation is already in `UI/Favorites/AddRoomView.swift`.
Update `UI/AddRoom/AddRoomView.swift` to redirect to the complete implementation:

1. Replace the stub in `UI/AddRoom/AddRoomView.swift` with a typealias to the canonical implementation in `UI/Favorites/AddRoomView.swift`
2. Verify that `MainTabView` still compiles because it references `AddRoomView(selectedTab:)`

The canonical implementation in `UI/Favorites/AddRoomView.swift` already has `@Binding var selectedTab: Tab` and all the business logic wired correctly.
  </action>
  <acceptance_criteria>
    - grep -n "typealias AddRoomView = DouyinLiveTVUI.Favorites.AddRoomView" C:\Users\59213\douyin\DouyinLiveTV\UI\AddRoom\AddRoomView.swift → 1 match
    - MainTabView still references AddRoomView and compiles
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' build | grep -E 'error:|BUILD SUCCEEDED'</automated>
  </verify>
  <done>
- Duplicate stub removed, only canonical implementation remains
- Project still compiles
  </done>
</task>

</tasks>

<verification>
All requirements should be satisfied after this plan:
- FAV-02: User can view list of saved favorite rooms ✓
- FAV-03: User can delete rooms from favorites ✓
- FAV-04: User can quickly select a favorite room to monitor ✓

The implementation follows all locked decisions from 05-CONTEXT.md:
- Plain List with full-width rows ✓
- Delete via edit mode ✓
- Immediate navigation on selection ✓
- Sort by lastViewed descending ✓
- Each cell shows all required information ✓
- Uses SwiftData @Query for automatic fetching ✓
</verification>

<success_criteria>
1. `FavoritesView` exists with full implementation replacing the stub
2. `FavoriteRoomCell` exists as a separate reusable component
3. `@Query` automatically fetches and sorts favorites by lastViewed descending
4. User can enter edit mode and delete rooms
5. Tapping a room navigates immediately to WatchLive tab with that room loading
6. Empty state displays correctly when no favorites saved
7. All interactive elements meet 88pt minimum focus target size
8. Code builds successfully with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-favorites-management/05-02-SUMMARY.md`
</output>
