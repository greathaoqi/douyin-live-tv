---
phase: 05-favorites-management
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - DouyinLiveTV/UI/Favorites/AddRoomView.swift
  - DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift
autonomous: true
requirements: [FAV-01]
user_setup: []

must_haves:
  truths:
    - "User can enter room ID or share URL in text field"
    - "App extracts room ID from multiple URL formats"
    - "App fetches room metadata from Douyin API"
    - "Room saves to favorites on success"
    - "After add completes successfully, navigates to Favorites tab"
  artifacts:
    - path: "DouyinLiveTV/UI/Favorites/AddRoomView.swift"
      provides: "Add room UI with text field and add button"
    - path: "DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift"
      provides: "Add room business logic and error handling"
  key_links:
    - from: "AddRoomViewModel"
      to: "FavoritesService"
      via: "extractRoomId + addRoom"
    - from: "AddRoomView"
      to: "MainTabView"
      via: "selectedTab navigation after success"
---

<objective>
Implement AddRoomView with text input, room ID extraction from multiple URL formats, API fetch, and save to favorites.

Per locked decisions: Extract room ID from raw ID, douyin.com/video/XXX, v.douyin.com/XXX, and douyin.com/user/XXX/live formats. After successful add, navigate to Favorites tab. Dictation already enabled per Phase 3.

Purpose: Allows users to add new favorite rooms.
Output: Complete AddRoomView with view model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-favorites-management/05-CONTEXT.md
@.planning/phases/05-favorites-management/05-UI-SPEC.md
@.planning/phases/05-favorites-management/05-01-PLAN.md
@C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift

<interfaces>
From 05-CONTEXT.md URL formats to support:
- Raw ID: "123456789" → "123456789"
- "https://www.douyin.com/video/XXX" → XXX
- "https://v.douyin.com/XXX" → XXX
- "https://www.douyin.com/user/XXX/live" → XXX

From 05-UI-SPEC.md errors copy:
- Invalid input: "Could not extract room ID from input. Please enter a valid room ID or share URL."
- API fetch error: "Could not find room. Please check the URL or ID and try again."

From 05-UI-SPEC.md copy:
- Primary CTA: "Add Room"

From 05-UI-SPEC.md spacing/typography:
- xs: 4pt, sm: 8pt, md: 16pt, lg: 24pt, xl: 32pt, 3xl: 64pt
- Body: 20pt, Label: 24pt, Heading: 28pt

Dictation: Already enabled at app level per Phase 3 (UX-05), nothing additional needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create AddRoomViewModel with extraction and add logic</name>
  <files>DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift
  </read_first>
  <action>
Create new file `DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift`:

- Import `Foundation`, `Observation`
- `@Observable class AddRoomViewModel`
- Properties:
  - `let favoritesService: FavoritesService`
  - `var inputText: String = ""`
  - `var isLoading: Bool = false`
  - `errorMessage: String? = nil`
  - `var addedRoom: LiveRoom? = nil`
- Init: `init(favoritesService: FavoritesService = DependencyContainer.shared.favoritesService)`
- Computed var `isInputValid: Bool`: returns true if `!inputText.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty`
- Method `func addRoom() async -> Bool`:
  1. Trim whitespace from inputText
  2. Call `favoritesService.extractRoomId(from: inputText)`
  3. If extract returns nil → set errorMessage to "Could not extract room ID..." (from UI-SPEC) → return false
  4. If extract succeeds → set isLoading = true, clear error
  5. Call `try await favoritesService.addRoom(roomId: extractedId)`
  6. On success → set addedRoom = room → return true
  7. On error → set errorMessage to "Could not find room..." (from UI-SPEC) → return false
  8. Always set isLoading = false at end

`FavoritesService.extractRoomId` already implemented in Plan 01 — just use it.
  </action>
  <acceptance_criteria>
    - File exists: `C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift`
    - `grep -c "@Observable class AddRoomViewModel" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift` → 1 match
    - `grep -E "inputText|isLoading|errorMessage|addRoom" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift` → all properties exist
    - `grep -E "favoritesService\\.extractRoomId" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift` → uses extraction from service
    - `grep -E "Could not extract room ID|Could not find room" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift` → error messages match contract
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' clean build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>AddRoomViewModel created with all business logic and error handling</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement AddRoomView UI with text field and add button</name>
  <files>DouyinLiveTV/UI/Favorites/AddRoomView.swift</files>
  <read_first>
    - .planning/phases/05-favorites-management/05-UI-SPEC.md
    - C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomViewModel.swift
  </read_first>
  <action>
Create new file `DouyinLiveTV/UI/Favorites/AddRoomView.swift`:

- Import SwiftUI
- `struct AddRoomView: View`
- Properties:
  - `@StateObject private var viewModel = AddRoomViewModel()`
  - `@Binding var selectedTab: Tab` (for navigation after success)
- Layout structure (VStack with spacing 3xl = 64pt):
  1. Heading: "Add Live Room to Favorites" (28pt semibold)
  2. Text field: bound to `viewModel.inputText`, placeholder: "Enter room ID or URL"
     - Text field height at least 88pt for focus target
  3. If `viewModel.errorMessage != nil`: show error message in red (20pt)
  4. Add button: "Add Room" (system plus icon), disabled when `!viewModel.isInputValid || viewModel.isLoading`
     - Button size minimum 88pt height
  5. When `viewModel.isLoading`: show progress indicator
- On appear: set focus to text field (tvOS)
- When `viewModel.addedRoom != nil`: automatically switch `selectedTab = .favorites` (per locked decision)
- Apply colors per 05-UI-SPEC: system background, accent for button, red for errors
- Add `focusable()` and `focusEffect()` to button
- Spacing: 64pt between main sections, 16pt between elements

AddRoomView needs `selectedTab: Binding<Tab>` from parent `MainTabView` for post-add navigation.
  </action>
  <acceptance_criteria>
    - File exists: `C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomView.swift`
    - `grep -E "@StateObject.*AddRoomViewModel" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomView.swift` → view model injected
    - `grep -E "selectedTab.*Binding.*Tab" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomView.swift` → selectedTab binding exists
    - `grep -E "selectedTab = .favorites" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomView.swift` → navigation to favorites after add
    - `grep -E "Add Room" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomView.swift` → add button labeled correctly
    - `grep -E "focusable.*focusEffect" C:/Users/59213/douyin/DouyinLiveTV/UI/Favorites/AddRoomView.swift` → focus effect configured
    - Error message displayed in red when present
    - Add button disabled when input invalid
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' build | grep -E 'error|BUILD SUCCEEDED'</automated>
  </verify>
  <done>AddRoomView created with input field, button, error handling, navigation</done>
</task>

</tasks>

<verification>
- Text field accepts input with dictation support
- Add button disabled for empty input
- Proper error messages shown for invalid input and API errors
- After successful add, app navigates to Favorites tab
- Project compiles without errors
</verification>

<success_criteria>
- User can enter room ID or any supported share URL
- Room ID extracts correctly from all four formats
- Room metadata fetched from Douyin API and saved to SwiftData
- Saved room appears in Favorites list after navigation
- All tvOS focus requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/05-favorites-management/05-03-SUMMARY.md`
</output>
