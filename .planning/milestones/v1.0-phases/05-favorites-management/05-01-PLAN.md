---
phase: 05-favorites-management
plan: 01
type: execute
wave: 1
depends_on: [05-00]
files_modified:
  - DouyinLiveTV/App/DependencyContainer.swift
  - DouyinLiveTV/Data/Services/FavoritesService.swift
autonomous: true
requirements: [FAV-06]
user_setup: []

must_haves:
  truths:
    - "SwiftData is configured with LiveRoom model"
    - "FavoritesService can perform all CRUD operations on favorites"
    - "Favorites persist across app restarts"
  artifacts:
    - path: "DouyinLiveTV/Data/Services/FavoritesService.swift"
      provides: "Favorites CRUD operations and last room tracking"
      exports: ["FavoritesService"]
      min_lines: 50
    - path: "DouyinLiveTV/App/DependencyContainer.swift"
      provides: "SwiftData model container with LiveRoom"
      contains: "LiveRoom"
  key_links:
    - from: "DependencyContainer"
      to: "FavoritesService"
      via: "initialization"
    - from: "FavoritesService"
      to: "SwiftData"
      via: "ModelContext"
      pattern: "modelContext\\.(fetch|insert|delete|save)"
---

<objective>
Create FavoritesService in Data layer and configure SwiftData model container with LiveRoom.

This establishes the persistence foundation for all favorites functionality.
Per locked decisions: Uses SwiftData for persistence, matches existing service patterns.

Purpose: Provides the data layer infrastructure needed by all UI features.
Output: `FavoritesService` implemented and registered in `DependencyContainer`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-favorites-management/05-CONTEXT.md
@C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift
@C:/Users/59213/douyin/DouyinLiveTV/Data/Services/LiveStatsService.swift
@C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift

<interfaces>
From DouyinLiveTV/Domain/Models/LiveRoom.swift:
```swift
@Model
class LiveRoom {
    @Attribute(.unique) var roomId: String
    var title: String
    var nickname: String
    var avatarUrl: String?
    var isLive: Bool
    var lastChecked: Date
    var lastViewed: Date?

    init(
        roomId: String,
        title: String,
        nickname: String,
        avatarUrl: String? = nil,
        isLive: Bool = false,
        lastChecked: Date = Date(),
        lastViewed: Date? = nil
    )
}
```

From DouyinLiveTV/App/DependencyContainer.swift (existing pattern):
```swift
class DependencyContainer {
    static let shared = DependencyContainer()
    let modelContainer: ModelContainer
    public let liveStatsService: LiveStatsService

    private init() {
        // modelContainer initialized here
        // ... services initialized here
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update DependencyContainer to add LiveRoom to SwiftData ModelContainer</name>
  <files>DouyinLiveTV/App/DependencyContainer.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift
  </read_first>
  <action>
Modify line 35 in `DependencyContainer.swift` where `modelContainer` is initialized:
- Change `for: []` to `for: [LiveRoom.self]`
- Add stored property `public let favoritesService: FavoritesService`
- In the init method after `liveStatsService` initialization, add:
  ```swift
  self.favoritesService = FavoritesService(
      modelContainer: self.modelContainer,
      liveStatsService: self.liveStatsService
  )
  ```
- Import `SwiftData` (already imported) and ensure `LiveRoom` is accessible.
  </action>
  <acceptance_criteria>
    - `grep -n "LiveRoom" C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift` contains "for: \\[LiveRoom.self\\]"
    - `grep -n "favoritesService" C:/Users/59213/douyin/DouyinLiveTV/App/DependencyContainer.swift` contains "public let favoritesService: FavoritesService"
  </acceptance_criteria>
  <verify>
    <automated>Project builds in Xcode (xcodebuild -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' clean build | grep -E 'error|BUILD SUCCEEDED')</automated>
  </verify>
  <done>LiveRoom is in SwiftData model container, favoritesService declared</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create FavoritesService with all required operations</name>
  <files>DouyinLiveTV/Data/Services/FavoritesService.swift</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Data/Services/LiveStatsService.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift
  </read_first>
  <action>
Create new file `DouyinLiveTV/Data/Services/FavoritesService.swift` with:

- Import `Foundation`, `SwiftData`, `DouyinLiveTVDomain`
- `public class FavoritesService` with these properties:
  - `private let modelContainer: ModelContainer`
  - `private let liveStatsService: LiveStatsService`
  - `@Published var favorites: [LiveRoom] = []`
- Public init that accepts `modelContainer: ModelContainer` and `liveStatsService: LiveStatsService`
- Implement these public methods:
  1. `func fetchFavorites() async throws -> [LiveRoom]` — fetch sorted by `lastViewed` descending (nil lastViewed sort after non-nil)
  2. `func addRoom(roomId: String) async throws -> LiveRoom` — fetch room metadata via `liveStatsService`, save to SwiftData, refresh favorites
  3. `func deleteRoom(_ room: LiveRoom) async throws` — delete from context, refresh favorites
  4. `func updateLastViewed(_ room: LiveRoom) async throws` — update `lastViewed` to `Date()`, save
  5. `func saveLastSelectedRoomId(_ roomId: String)` — store in `UserDefaults.standard` for app launch recall
  6. `func getLastSelectedRoomId() -> String?` — retrieve from `UserDefaults.standard`
  7. `func extractRoomId(from input: String) -> String?` — implements URL parsing per decision:
     - Raw numeric string: return as-is
     - `https://www.douyin.com/video/XXX` → extract XXX
     - `https://v.douyin.com/XXX` → extract XXX
     - `https://www.douyin.com/user/XXX/live` → extract XXX
     - Trims whitespace, returns nil if nothing extracted
- After any write operation (add/delete/update), refetch favorites and update `@Published` property

Follow the existing code style: `public` ACL for all methods that UI will use, `private` for internals.
  </action>
  <acceptance_criteria>
    - File exists: `C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift`
    - `grep -c "class FavoritesService" C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift` → 1 match
    - `grep -E "(fetchFavorites|addRoom|deleteRoom|updateLastViewed|extractRoomId)" C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift` → all 5 methods exist
    - `grep -E "UserDefaults|lastSelectedRoomId" C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift` → last room persistence implemented
    - `grep -E "modelContext\\.(insert|delete|save)" C:/Users/59213/douyin/DouyinLiveTV/Data/Services/FavoritesService.swift` → SwiftData operations used
  </acceptance_criteria>
  <verify>
    <automated>Project builds in Xcode (same build command as task 1)</automated>
  </verify>
  <done>FavoritesService exists with all CRUD operations and last room tracking</done>
</task>

</tasks>

<verification>
- Project compiles with no errors
- SwiftData configured correctly with LiveRoom model
- All CRUD operations declared in FavoritesService
- Last room persistence stored in UserDefaults
</verification>

<success_criteria>
- DependencyContainer has favoritesService and LiveRoom in model container
- FavoritesService implements all required data operations
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-favorites-management/05-01-SUMMARY.md`
</output>
