---
phase: 05-favorites-management
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-03, 05-04]
files_modified:
  - DouyinLiveTV/UI/Favorites/FavoritesView.swift
  - DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift
autonomous: true
requirements: [FAV-02, FAV-03, FAV-04]
user_setup: []

must_haves:
  truths:
    - "User can view a list of all saved favorite rooms"
    - "User can delete rooms from favorites via edit mode"
    - "User can quickly select a favorite room to monitor"
  artifacts:
    - path: "DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift"
      provides: "Individual cell rendering with room title, author, live indicator, last checked time"
      min_lines: 40
    - path: "DouyinLiveTV/UI/Favorites/FavoritesView.swift"
      provides: "Main favorites list UI with @Query, List, edit mode delete, selection handling"
      contains: "@Query"
      contains: "List"
      contains: "onDelete"
      contains: "editMode"
  key_links:
    - from: "FavoritesView.swift"
      to: "SwiftData"
      via: "@Query(sort: \\LiveRoom.lastViewed, order: .reverse)"
      pattern: "@Query.*LiveRoom"
    - from: "FavoriteRoomCell.swift"
      to: "LiveRoom"
      via: "LiveRoom parameter to render"
      pattern: "LiveRoom"
    - from: "FavoritesView.swift"
      to: "MainTabView"
      via: "selectedTab binding navigation to .watchLive"
      pattern: "selectedTab.*watchLive"
---

<objective>
Complete the favorites list UI implementation to address verification gaps. This implements viewing the favorites list, edit mode delete, and cell selection with immediate navigation to the watch live tab.

Purpose: Fulfills the three remaining requirements (FAV-02, FAV-03, FAV-04) that were blocked by missing UI implementation.
Output: Complete favorites list with all interaction features working as per design contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-favorites-management/05-CONTEXT.md
@.planning/phases/05-favorites-management/05-RESEARCH.md
@.planning/phases/05-favorites-management/05-UI-SPEC.md
@.planning/phases/05-favorites-management/05-VERIFICATION.md
@DouyinLiveTV/Domain/Models/LiveRoom.swift
@DouyinLiveTV/Data/Services/FavoritesService.swift

<interfaces>
From LiveRoom.swift:
```swift
@Model
class LiveRoom {
    var roomId: String
    var title: String
    var nickname: String
    var avatarUrl: String?
    var isLive: Bool
    var lastChecked: Date
    var lastViewed: Date?
}
```

From existing FavoritesView stub:
```swift
struct FavoritesView: View {
    @Binding var selectedTab: Tab
    var body: some View { ... } // stub only empty state
}
```

From CONTEXT.md decisions:
- Plain List with full-width rows (standard tvOS pattern)
- Each cell shows: room title, author nickname, colored live indicator, last checked time
- Sort by lastViewed descending (most recently used at top)
- Delete via edit mode (standard tvOS pattern)
- Immediate navigation to WatchLive tab on selection
- All interactive items must be 88pt minimum height with focusEffect
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FavoriteRoomCell component</name>
  <files>DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift</files>
  <read_first>
    - DouyinLiveTV/UI/Favorites/FavoritesView.swift
    - DouyinLiveTV/Domain/Models/LiveRoom.swift
    - .planning/phases/05-favorites-management/05-UI-SPEC.md
  </read_first>
  <action>
Create `FavoriteRoomCell.swift` in `DouyinLiveTV/UI/Favorites/` that renders a single live room row following these specifications:
- Parameters: `let room: LiveRoom`
- Layout: HStack with content on leading side, live indicator and last checked time on trailing side
- Content:
  - Room title: 28pt semibold (Heading typography from 05-UI-SPEC)
  - Author nickname: 24pt regular (Label typography), secondary color
  - Live indicator: Circle shape filled green when `room.isLive == true`, gray when false
  - Last checked time: Format as "Checked: [relative time]" (e.g., "Checked: 5m ago", "Checked: 2h ago") using 20pt regular Body typography, secondary color
- Spacing: Follow 05-UI-SPEC spacing scale: 16pt (md) between elements, 24pt (lg) vertical padding
- Focus: Minimum 88pt height, `focusable()`, `focusEffect()` modifier
- Background: `secondarySystemBackground` with corner radius 8pt (sm)
</action>
  <acceptance_criteria>
  - File exists at path: DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift
  - File contains `struct FavoriteRoomCell: View`
  - File contains `let room: LiveRoom` stored property
  - File contains `.focusable()` modifier
  - File contains `.focusEffect()` modifier
  - Minimum row height is >= 88pt
  - Conditional green/gray circle for live status indicator exists
</acceptance_criteria>
  <verify>
    <automated>grep -n "struct FavoriteRoomCell\|let room: LiveRoom\|.focusable\|.focusEffect\|Circle" DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift</automated>
  </verify>
  <done>FavoriteRoomCell created with all required UI elements and proper focus handling</done>
</task>

<task type="auto">
  <name>Task 2: Complete FavoritesView implementation with list, edit mode, and selection</name>
  <files>DouyinLiveTV/UI/Favorites/FavoritesView.swift</files>
  <read_first>
    - DouyinLiveTV/UI/Favorites/FavoritesView.swift
    - DouyinLiveTV/UI/Favorites/FavoriteRoomCell.swift
    - DouyinLiveTV/Data/Services/FavoritesService.swift
    - DouyinLiveTV/App/DependencyContainer.swift
    - .planning/phases/05-favorites-management/05-CONTEXT.md
  </read_first>
  <action>
Replace the existing stub in `FavoritesView.swift` with full implementation following these specifications:

1. **Properties**:
   - Keep existing `@Binding var selectedTab: Tab`
   - Add `@Query(sort: \LiveRoom.lastViewed, order: .reverse) private var rooms: [LiveRoom]` (per sort decision in CONTEXT)
   - Add `@Environment(\.modelContext) private var modelContext`
   - Add `@State private var isEditing = false` for edit mode
   - Get `favoritesService` from `DependencyContainer.shared`

2. **Body Structure**:
   - If `rooms.isEmpty`: Show existing empty state text (keep as-is)
   - Else: Show `List` with `ForEach(rooms) { room in FavoriteRoomCell(room: room) }`
   - Add edit button in navigation bar that toggles `isEditing` (shows "Done" when editing, "Edit" when not editing)
   - Attach `.environment(\.editMode, $isEditing)` to the List
   - Implement `onDelete` at the ForEach level that calls `try? await favoritesService.deleteRoom(room)`
   - Enable `focusable` on the edit button with `focusEffect`

3. **Selection Behavior**:
   - Add `onTapGesture` to cell that:
     1. Calls `try? await favoritesService.updateLastViewed(room)` (updates sort order)
     2. Calls `favoritesService.saveLastSelectedRoomId(room.roomId)`
     3. Sets `selectedTab = .watchLive(room.roomId)` (navigation to watch live tab)
   - Ensure cell selection meets minimum 88pt focus target requirement

4. **tvOS Compliance**:
   - All interactive elements (cells, edit button) have `focusable()` and `focusEffect()`
   - Edit button meets 88pt minimum size requirement

5. **Empty State**: Keep the existing empty state as fallback when no rooms are favorited.
</action>
  <acceptance_criteria>
  - File contains `@Query(sort: \LiveRoom.lastViewed, order: .reverse)`
  - File contains `@Environment(\.modelContext)`
  - File contains `@State private var isEditing = false`
  - File contains `List` and `ForEach`
  - File contains `.environment(\.editMode, $isEditing)`
  - File contains `onDelete(perform: deleteRoom)`
  - File contains `selectedTab = .watchLive` when cell tapped
  - File calls `favoritesService.updateLastViewed` on selection
  - File calls `favoritesService.saveLastSelectedRoomId` on selection
  - Edit button has `.focusable()` and `.focusEffect()`
</acceptance_criteria>
  <verify>
    <automated>grep -n "@Query\|@Environment.*modelContext\|editMode\|onDelete\|selectedTab.*watchLive" DouyinLiveTV/UI/Favorites/FavoritesView.swift</automated>
  </verify>
  <done>FavoritesView fully implemented with list viewing, edit mode deletion, and selection navigation</done>
</task>

</tasks>

<verification>
All three gap truths will be verifiable after this plan completes:
1. User can view list of saved favorite rooms → List with @Query fetches from SwiftData, FavoriteRoomCell renders each row
2. User can delete rooms via edit mode → Edit button toggles edit mode, onDelete calls FavoritesService.deleteRoom
3. User can select favorite to monitor → Tap cell updates lastViewed, saves to UserDefaults, navigates to watch live tab

Existing verified items remain valid:
- FAV-01 (add room): already complete
- FAV-05 (last room recall): already complete
- FAV-06 (SwiftData persistence): already complete
</verification>

<success_criteria>
- All 6 FAV requirements satisfied: 3 previously complete + 3 completed by this plan
- User can view all favorites in a list sorted by most recently used
- User can enter edit mode and delete any favorite room
- Tapping a favorite cell immediately navigates to the watch live tab with that room open
- Favorites persist across app restarts (already verified, no change)
- All interactive elements meet tvOS 88pt minimum focus target requirement
- All tap/select/delete interactions work with Siri Remote
</success_criteria>

<output>
After completion, create `.planning/phases/05-favorites-management/05-05-SUMMARY.md` with completion summary.
</output>
