---
phase: 04-live-room-core-display
plan: 3
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - "DouyinLiveTV/UI/WatchLive/WatchLiveView.swift"
  - "DouyinLiveTVTests/LiveRoomDisplayTests.swift"
autonomous: false
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-06, LIVE-07]
user_setup: []

must_haves:
  truths:
    - "Full-screen video playback via native SwiftUI VideoPlayer (LIVE-02)"
    - "Statistics overlay displayed on top of video (LIVE-03)"
    - "Toggle button hides/shows overlay animated (LIVE-06)"
    - "Empty state shown when no room selected"
    - "Error state shown when loading fails"
    - "PiP available automatically when app goes to background (LIVE-07)"
  artifacts:
    - path: "DouyinLiveTV/UI/WatchLive/WatchLiveView.swift"
      provides: "Main live room view that composes all components"
      contains: "ZStack"
      contains: "VideoPlayer"
      contains: "StatisticsOverlay"
      contains: "OverlayToggleButton"
    - path: "DouyinLiveTVTests/LiveRoomDisplayTests.swift"
      provides: "UI contract validation tests"
      contains: "testTextSizesMeetsRequirements"
      contains: "testStatusIndicatorColor"
  key_links:
    - from: "WatchLiveView"
      to: "LiveRoomViewModel"
      via: "@StateObject"
    - from: "WatchLiveView"
      to: "StatisticsOverlay"
      via: "ZStack overlay"
    - from: "WatchLiveView"
      to: "VideoPlayer"
      via: "native usage"
    - from: "VideoPlayer"
      to: "PlayerService.player"
      via: "initialization"
---

<objective>
Implement the main WatchLiveView that integrates all components: full-screen video, statistics overlay, toggle button, with proper animation and empty/error states.

Purpose: This is the main user-facing screen for the core feature - watching a live room with stats overlay. It composes the services, view model, and components created in previous plans into a complete feature.

Output: Complete WatchLiveView implementation, UI contract validation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-live-room-core-display/04-CONTEXT.md
@.planning/phases/04-live-room-core-display/04-RESEARCH.md
@.planning/phases/04-live-room-core-display/04-UI-SPEC.md
@DouyinLiveTV/UI/WatchLive/WatchLiveView.swift (existing placeholder)
@DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift
@DouyinLiveTV/UI/WatchLive/StatisticsOverlay.swift
@DouyinLiveTV/UI/WatchLive/OverlayToggleButton.swift
@DouyinLiveTV/Domain/Services/PlayerService.swift
@DouyinLiveTV/App/DependencyContainer.swift

<interfaces>
From 04-CONTEXT.md layout decisions:
- Overall layout: ZStack with full-screen VideoPlayer at back, overlay + toggle on top (LIVE-03)
- Video gravity: .resizeAspectFill (fills screen for 9:16 vertical streams)
- Overlay position: top-leading corner with safe area padding
- Toggle position: top-trailing corner with safe area padding
- Animation: Animate overlay toggle with fade (opacity)
- Auto-play: Video starts playing automatically when room loads

From 04-UI-SPEC.md copy:
- Empty state heading: "No Live Room Selected"
- Empty state body: "Select a live room from favorites or add a new one to start monitoring."
- Error state: "Could not load live room data. Check the room ID and try again."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UI contract validation tests</name>
  <files>DouyinLiveTVTests/LiveRoomDisplayTests.swift</files>
  <read_first>
    - .planning/phases/04-live-room-core-display/04-UI-SPEC.md
    - DouyinLiveTV/UI/WatchLive/StatisticsOverlay.swift
  </read_first>
  <action>
Create `DouyinLiveTVTests/LiveRoomDisplayTests.swift`:
- Add test `testTextSizesMeetsRequirements`: verify that StatisticsOverlay uses 36pt for labels and 48pt for values (LIVE-04)
- Add test `testStatusIndicatorColor`: green for live, red for offline (LIVE-05)
  </action>
  <verify>
    <automated>xcodebuild test -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' ONLY_TESTING=DouyinLiveTVTests/LiveRoomDisplayTests | grep -E "(FAILED|PASSED|ERROR)"</automated>
  </verify>
  <acceptance_criteria>
    - File "DouyinLiveTVTests/LiveRoomDisplayTests.swift" exists
    - Contains test "testTextSizesMeetsRequirements"
    - Contains test "testStatusIndicatorColor"
    - All tests in LiveRoomDisplayTests pass
  </acceptance_criteria>
  <done>
UI contract validation tests created and passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement WatchLiveView with ZStack layout</name>
  <files>DouyinLiveTV/UI/WatchLive/WatchLiveView.swift</files>
  <read_first>
    - DouyinLiveTV/UI/WatchLive/WatchLiveView.swift (existing placeholder)
    - DouyinLiveTV/UI/WatchLive/LiveRoomViewModel.swift
    - DouyinLiveTV/UI/WatchLive/StatisticsOverlay.swift
    - DouyinLiveTV/UI/WatchLive/OverlayToggleButton.swift
    - DouyinLiveTV/App/DependencyContainer.swift
    - .planning/phases/04-live-room-core-display/04-CONTEXT.md
  </read_first>
  <action>
Replace the placeholder `WatchLiveView` in `DouyinLiveTV/UI/WatchLive/WatchLiveView.swift` with full implementation:
- Use `@StateObject private var viewModel: LiveRoomViewModel` injected from DependencyContainer
  (Get `playerService`, `liveStatsService`, `appLifecycleService` from `DependencyContainer.shared`)
- Main layout: `ZStack(alignment: .topLeading)` (LIVE-03):
  1. Background: `Color.black`
  2. Full-screen VideoPlayer: `VideoPlayer(player: viewModel.playerService.player)`
     - Apply `.scaledToFill()` → `.resizeAspectFill` (per CONTEXT.md for 9:16 streams)
     - Apply `.ignoresSafeArea()`
  3. When viewModel.showOverlay = true AND viewModel.stats != nil → show `StatisticsOverlay(stats: viewModel.stats!, roomTitle: viewModel.roomTitle)` in top-leading
     - Add `.padding(.safeAreaPadding)` to respect overscan (avoids pitfall documented in research)
     - Animate opacity: `opacity(viewModel.showOverlay ? 1 : 0)` with `.animation(.default, value: viewModel.showOverlay)`
  4. `OverlayToggleButton` in top-trailing corner (use `.frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topTrailing)` container)
     - Add `.padding(.safeAreaPadding)`
     - Action: `viewModel.toggleOverlay()` (LIVE-06)
- Handle empty state (when no room selected, stats is nil):
  - Show centered VStack with heading (36pt) and body (28pt) per 04-UI-SPEC.md copy
- Handle loading state:
  - Show `ProgressView` centered when `viewModel.isLoading = true`
- Handle error state:
  - Show error message in red when `viewModel.errorMessage != nil` per copy contract
- Store last viewed room ID in `UserDefaults.standard` when room loads successfully (per research recommendation, temporary until Phase 5 SwiftData)
- Add `#Preview` with a mock room for Xcode previews
  </action>
  <verify>
    <automated>grep -n "ZStack" "DouyinLiveTV/UI/WatchLive/WatchLiveView.swift" && grep -n "VideoPlayer" "DouyinLiveTV/UI/WatchLive/WatchLiveView.swift" && grep -n "StatisticsOverlay" "DouyinLiveTV/UI/WatchLive/WatchLiveView.swift"</automated>
  </verify>
  <acceptance_criteria>
    - Replaces placeholder implementation in WatchLiveView.swift
    - Contains `ZStack(alignment: .topLeading)` for video + overlay layout
    - Contains native `VideoPlayer` from SwiftUI (LIVE-02)
    - Contains `StatisticsOverlay` when overlay should be shown (LIVE-03)
    - Contains `OverlayToggleButton` in top-trailing corner (LIVE-06)
    - `VideoPlayer` has `scaledToFill()` (resizeAspectFill per requirement)
    - Overlay has animated opacity when toggled (LIVE-06)
    - Empty state displayed when no room selected with correct copy
    - Error state displayed with correct copy when loading fails
    - Both overlay and toggle button respect safe area padding with safeAreaPadding
    - Contains `#Preview` for Xcode previews
  </acceptance_criteria>
  <done>
WatchLiveView implemented with full integration of all components (LIVE-01, LIVE-02, LIVE-03, LIVE-06 complete).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete feature</name>
  <files>DouyinLiveTV/UI/WatchLive/WatchLiveView.swift</files>
  <read_first>
    - .planning/phases/04-live-room-core-display/04-CONTEXT.md
    - .planning/phases/04-live-room-core-display/04-UI-SPEC.md
  </read_first>
  <action>
No code action needed - all implementation completed in tasks 1 and 2. This task waits for human verification of the complete feature to confirm all requirements work correctly on actual hardware/simulator.
  </action>
  <verify>
    <automated>N/A - manual visual and interaction verification required</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms video plays full-screen
    - Human confirms statistics overlay displays correctly with 36pt/48pt font
    - Human confirms overlay toggle works with animation
    - Human confirms Picture in Picture starts automatically when app goes to background
    - Human confirms live/offline indicator is correct color
  </acceptance_criteria>
  <done>
Human has verified the complete feature works as expected. All requirements satisfied.
  </done>
</task>

</tasks>

<verification>
All unit tests pass. Project builds successfully. UI contract tests verify font sizes and colors match the design spec. Human verification confirms the complete feature works as expected.
</verification>

<success_criteria>
All 7 requirements satisfied:
- LIVE-01: Basic statistics displayed ✓
- LIVE-02: Live video preview via AVPlayer ✓
- LIVE-03: Stats overlay on top of video ✓
- LIVE-04: Large text sizing for couch reading ✓
- LIVE-05: Visual live/offline indicator ✓
- LIVE-06: Toggle between overlay and full-screen ✓
- LIVE-07: Picture in Picture support ✓
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-room-core-display/04-03-SUMMARY.md`
</output>
