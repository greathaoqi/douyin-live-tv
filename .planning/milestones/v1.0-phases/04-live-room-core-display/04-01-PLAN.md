---
phase: 04-live-room-core-display
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "DouyinLiveTV/App/DependencyContainer.swift"
  - "DouyinLiveTV/Domain/Services/PlayerService.swift"
  - "DouyinLiveTV/Data/Services/LiveStatsService.swift"
  - "DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift"
  - "DouyinLiveTVTests/LiveStatsServiceTests.swift"
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-07]
user_setup: []

must_haves:
  truths:
    - "AVPlayer instance is managed by a separate service for lifecycle control"
    - "Live stats can be fetched from Douyin API via LiveStatsService"
    - "Picture in Picture is enabled on player creation"
  artifacts:
    - path: "DouyinLiveTV/Domain/Services/PlayerService.swift"
      provides: "AVPlayer lifecycle management"
      exports: ["PlayerService"]
      contains: "class PlayerService"
    - path: "DouyinLiveTV/Data/Services/LiveStatsService.swift"
      provides: "Live statistics fetching from API"
      exports: ["LiveStatsService"]
      contains: "func fetchStats(for roomId: String)"
    - path: "DouyinLiveTV/App/DependencyContainer.swift"
      provides: "Dependency registration"
      contains: "playerService"
      contains: "liveStatsService"
  key_links:
    - from: "DependencyContainer"
      to: "PlayerService"
      via: "property initialization"
    - from: "LiveStatsService"
      to: "APIClient"
      via: "API request for live stats"
      pattern: "apiClient.request"
---

<objective>
Create core domain services for video playback and live statistics fetching.

Purpose: Establishes the foundational services needed before building the UI. PlayerService manages AVPlayer lifecycle and Picture in Picture configuration. LiveStatsService fetches current statistics from the Douyin API. Both are registered in the DependencyContainer for injection.

Output: PlayerService.swift, LiveStatsService.swift, updated DependencyContainer.swift
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-live-room-core-display/04-CONTEXT.md
@.planning/phases/04-live-room-core-display/04-RESEARCH.md
@DouyinLiveTV/App/DependencyContainer.swift
@DouyinLiveTV/Domain/Models/LiveStats.swift
@DouyinLiveTV/Data/Network/APIClient.swift

<interfaces>
From DouyinLiveTV/Domain/Models/LiveStats.swift:
```swift
struct LiveStats: Codable, Equatable {
    let viewerCount: Int
    let likeCount: Int
    let totalGiftValue: Int
    let isLive: Bool
    let startTime: Date?
}
```

From DouyinLiveTV/App/DependencyContainer.swift (existing):
```swift
class DependencyContainer {
    static let shared = DependencyContainer()
    let modelContainer: ModelContainer
    let tokenStorage: TokenStorage
    let apiClient: APIClient
    let authService: AuthService
    let authStateManager: AuthStateManager
    let appLifecycleService: AppLifecycleService
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PlayerService in Domain layer</name>
  <files>DouyinLiveTV/Domain/Services/PlayerService.swift</files>
  <read_first>
    - DouyinLiveTV/App/DependencyContainer.swift
    - DouyinLiveTV/Data/Services/AppLifecycleService.swift
  </read_first>
  <behavior>
    - PlayerService has `@Published var player: AVPlayer?`
    - PlayerService has `@Published var isPlaying: Bool = false`
    - `loadVideo(url: URL)` creates AVPlayer, enables allowsPictureInPicturePlayback = true per D-07
    - `play()` starts playback if player exists
    - `pause()` pauses playback if player exists
    - `resume()` resumes playback if player exists
  </behavior>
  <action>
Create `DouyinLiveTV/Domain/Services/PlayerService.swift` with:
- Import AVKit and Combine
- `PlayerService` is `ObservableObject`
- Stored `player: AVPlayer?` as `@Published`
- Stored `isPlaying: Bool` as `@Published`
- `loadVideo(url: URL)`: creates new AVPlayer, sets `player.allowsPictureInPicturePlayback = true` (required for LIVE-07 PiP support)
- `func play()` → calls `player?.play()` and sets `isPlaying = true`
- `func pause()` → calls `player?.pause()` and sets `isPlaying = false`
- `func resume()` → calls `player?.play()` and sets `isPlaying = true`
- `func reset()` → sets `player = nil`, `isPlaying = false`

Follow existing code formatting patterns from `AppLifecycleService.swift`.
  </action>
  <verify>
    <automated>grep -n "class PlayerService" "DouyinLiveTV/Domain/Services/PlayerService.swift" && grep -n "allowsPictureInPicturePlayback = true" "DouyinLiveTV/Domain/Services/PlayerService.swift"</automated>
  </verify>
  <acceptance_criteria>
    - File "DouyinLiveTV/Domain/Services/PlayerService.swift" exists
    - Contains "import AVKit" and "import Combine"
    - Contains "class PlayerService: ObservableObject"
    - Contains "@Published var player: AVPlayer?"
    - Contains "@Published var isPlaying: Bool = false"
    - Contains "func loadVideo(url: URL)"
    - Contains "player.allowsPictureInPicturePlayback = true"
    - Contains "func play()", "func pause()", "func resume()", "func reset()"
  </acceptance_criteria>
  <done>
PlayerService created with AVPlayer management and PiP enabled.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LiveStatsService in Data layer</name>
  <files>DouyinLiveTV/Data/Services/LiveStatsService.swift, DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift, DouyinLiveTVTests/LiveStatsServiceTests.swift</files>
  <read_first>
    - DouyinLiveTV/Domain/Models/LiveStats.swift
    - DouyinLiveTV/Data/Network/APIClient.swift
    - DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift
  </read_first>
  <behavior>
    - LiveStatsService uses existing APIClient to fetch stats
    - `fetchStats(for roomId: String)` returns `LiveStats` on success
    - Propagates API errors to caller
    - Adds endpoint for getting live room stats
  </behavior>
  <action>
1. Create `DouyinLiveTV/Data/Services/LiveStatsService.swift`:
   - Initialize with an `APIClient` instance (injected)
   - Add method `func fetchStats(for roomId: String) async throws -> LiveStats`
   - Create endpoint request to Douyin API for live room statistics
   - Use `apiClient.request()` to perform request and return decoded `LiveStats`

2. Add endpoint extension to `DouyinLiveTV/Data/Network/DouyinAPIEndpoints.swift`:
   - Add `public static func getLiveStats(roomId: String, baseURL: URL = URL(string: "https://api.douyin.com")!) -> Endpoint<LiveStats>`
   - Use GET method, add roomId as query parameter
   - Add standard accept and user-agent headers matching existing patterns

3. Create unit test file `DouyinLiveTVTests/LiveStatsServiceTests.swift`:
   - Test successful decoding of LiveStats with all three values (viewerCount, likeCount, totalGiftValue)
   - Test that API errors are properly propagated
   - Test that isLive flag is correctly decoded
  </action>
  <verify>
    <automated>xcodebuild test -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation)' ONLY_TESTING=DouyinLiveTVTests/LiveStatsServiceTests | grep -E "(FAILED|PASSED|ERROR)"</automated>
  </verify>
  <acceptance_criteria>
    - File "DouyinLiveTV/Data/Services/LiveStatsService.swift" exists
    - Contains "class LiveStatsService" with "let apiClient: APIClient"
    - Contains "func fetchStats(for roomId: String) async throws -> LiveStats"
    - DouyinAPIEndpoints.swift contains "getLiveStats" endpoint definition
    - File "DouyinLiveTVTests/LiveStatsServiceTests.swift" exists with at least one test
    - All tests for LiveStatsService pass
  </acceptance_criteria>
  <done>
LiveStatsService created with API integration and unit tests. Unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register services in DependencyContainer</name>
  <files>DouyinLiveTV/App/DependencyContainer.swift</files>
  <read_first>
    - DouyinLiveTV/App/DependencyContainer.swift
  </read_first>
  <action>
Update `DependencyContainer` in `DouyinLiveTV/App/DependencyContainer.swift`:
- Add public stored properties:
  ```swift
  public let playerService: PlayerService
  public let liveStatsService: LiveStatsService
  ```
- Initialize in `private init()` after other services:
  ```swift
  self.playerService = PlayerService()
  self.liveStatsService = LiveStatsService(apiClient: self.apiClient)
  ```
  </action>
  <verify>
    <automated>grep -n "playerService" "DouyinLiveTV/App/DependencyContainer.swift" && grep -n "liveStatsService" "DouyinLiveTV/App/DependencyContainer.swift"</automated>
  </verify>
  <acceptance_criteria>
    - DependencyContainer contains public property `playerService: PlayerService`
    - DependencyContainer contains public property `liveStatsService: LiveStatsService`
    - Both properties are initialized in the init method
  </acceptance_criteria>
  <done>
Both services registered in DependencyContainer ready for injection.
  </done>
</task>

</tasks>

<verification>
All unit tests pass. Project builds without errors. Services registered and ready for use by view model and UI.
</verification>

<success_criteria>
- PlayerService manages AVPlayer lifecycle with PiP enabled (LIVE-02, LIVE-07)
- LiveStatsService can fetch statistics from API (LIVE-01)
- Both services available via DependencyContainer
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-room-core-display/04-01-SUMMARY.md`
</output>
