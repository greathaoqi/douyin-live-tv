---
phase: 06-refresh-quality-of-life
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - DouyinLiveTV/Data/Services/RefreshService.swift
  - DouyinLiveTV/App/DependencyContainer.swift
  - DouyinLiveTV/App/DouyinLiveTVApp.swift
  - DouyinLiveTV/Info.plist
autonomous: true
requirements: [REFRESH-02, REFRESH-03]
user_setup: []

must_haves:
  truths:
    - "App automatically refreshes favorite room stats every 30 minutes when in foreground"
    - "App uses BackgroundTasks framework to schedule background refresh"
    - "Background refresh updates all favorite rooms' isLive status and lastChecked in SwiftData"
    - "App respects foreground/background state for scheduling"
  artifacts:
    - path: "DouyinLiveTV/Data/Services/RefreshService.swift"
      provides: "RefreshService managing foreground timer and BackgroundTasks integration"
      exports: ["class RefreshService"]
    - path: "DouyinLiveTV/App/DependencyContainer.swift"
      provides: "Register RefreshService as shared instance"
      contains: "let refreshService: RefreshService"
    - path: "DouyinLiveTV/Info.plist"
      provides: "Enable background refresh capability"
      contains: "BGAppRefreshTask"
  key_links:
    - from: "DouyinLiveTVApp"
      to: "DependencyContainer.shared.refreshService"
      via: "application(_:didFinishLaunchingWithOptions:)"
      pattern: "refreshService.registerBackgroundRefresh"
    - from: "RefreshService"
      to: "FavoritesService"
      via: "fetchFavorites and update each room"
      pattern: "liveStatsService.fetchStats"
    - from: "RefreshService"
      to: "BackgroundTasks framework"
      via: "BGAppRefreshTask scheduler"
      pattern: "BGAppRefreshTask"
---

<objective>
Implement automatic refresh using BackgroundTasks framework for background updates
and timer for foreground updates.

Purpose: Keeps favorite room data (especially live status) up-to-date even when the app
isn't open, so when the user launches the app they see current information.

Output: Complete RefreshService that coordinates automatic refresh in both foreground and background.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-refresh-quality-of-life/06-CONTEXT.md
@.planning/phases/06-refresh-quality-of-life/06-UI-SPEC.md

<interfaces>
From DouyinLiveTV/App/DependencyContainer.swift:
```swift
class DependencyContainer {
    static let shared = DependencyContainer()
    public let favoritesService: FavoritesService
    public let liveStatsService: LiveStatsService
    // ... other services
}
```

From DouyinLiveTV/Data/Services/FavoritesService.swift:
```swift
public class FavoritesService {
    public func fetchFavorites() async throws -> [LiveRoom]
}
```

From DouyinLiveTV/Data/Services/LiveStatsService.swift:
```swift
public class LiveStatsService {
    public func fetchStats(for roomId: String) async throws -> LiveStats
}
```

From DouyinLiveTV/Domain/Models/LiveRoom.swift:
```swift
@Model class LiveRoom {
    var roomId: String
    var isLive: Bool
    var lastChecked: Date
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RefreshService with BackgroundTasks integration</name>
  <files>DouyinLiveTV/Data/Services/RefreshService.swift</files>
  <read_first>
    DouyinLiveTV/App/DependencyContainer.swift
    DouyinLiveTV/Data/Services/FavoritesService.swift
    DouyinLiveTV/Data/Services/AppLifecycleService.swift
  </read_first>
  <action>
Create new file `DouyinLiveTV/Data/Services/RefreshService.swift` with:

1. Import `BackgroundTasks` framework
2. Create `public class RefreshService` that has:
   - Dependencies: `FavoritesService`, `LiveStatsService`, `AppLifecycleService`
   - Private `Timer?` for foreground refresh
   - `private let refreshInterval: TimeInterval = 30 * 60` (30 minutes per requirement)

3. Public methods:
   - `init(favoritesService:liveStatsService:appLifecycleService:)`
   - `func registerBackgroundRefresh()` - called on app launch to register with BGTaskScheduler
   - `func scheduleNextRefresh()` - schedules next background refresh
   - `func handleBackgroundRefresh(_ task: BGAppRefreshTask)` - handles background refresh execution
   - `func startForegroundRefresh()` - starts timer when app enters foreground
   - `func stopForegroundRefresh()` - invalidates timer when app enters background
   - `func refreshAllFavorites() async throws` - fetches all favorites from SwiftData, updates each room with fresh stats and isLive status

4. Implementation details:
   - In `handleBackgroundRefresh`: after refreshing all favorites, schedule next refresh before completing task
   - In `refreshAllFavorites`: for each favorite room, call `liveStatsService.fetchStats`, update `room.isLive` and `room.lastChecked`, save to SwiftData
   - Observe app lifecycle state from `AppLifecycleService` to automatically start/stop foreground timer
   - Follow existing Combine pattern like `AppLifecycleService` uses for lifecycle observation

Follow decision D-02 (from 06-CONTEXT): Combine foreground timer (when app is in foreground) + BGAppRefreshTask (when in background).
  </action>
  <verify>
    <automated>ls DouyinLiveTV/Data/Services/RefreshService.swift && grep -n "class RefreshService" DouyinLiveTV/Data/Services/RefreshService.swift</automated>
  </verify>
  <done>
    - File `DouyinLiveTV/Data/Services/RefreshService.swift` exists
    - Contains `import BackgroundTasks`
    - Contains `BGAppRefreshTask` handling
    - Contains 30-minute refresh interval `30 * 60`
    - Contains `func refreshAllFavorites() async throws` that updates all favorites
    - Observes app lifecycle to manage foreground timer
    - Schedules next refresh after completion
  </done>
</task>

<task type="auto">
  <name>Task 2: Register RefreshService in DependencyContainer and app entry point</name>
  <files>DouyinLiveTV/App/DependencyContainer.swift, DouyinLiveTV/App/DouyinLiveTVApp.swift, DouyinLiveTV/Info.plist</files>
  <read_first>
    DouyinLiveTV/App/DependencyContainer.swift
    DouyinLiveTV/App/DouyinLiveTVApp.swift
    DouyinLiveTV/Info.plist
  </read_first>
  <action>
1. Add `public let refreshService: RefreshService` to `DependencyContainer` class
2. In `private init()` of DependencyContainer, initialize it after favoritesService:
```swift
self.refreshService = RefreshService(
    favoritesService: self.favoritesService,
    liveStatsService: self.liveStatsService,
    appLifecycleService: self.appLifecycleService
)
```

3. In `DouyinLiveTVApp` in `init()`, call:
```swift
// Register background refresh on app launch
DependencyContainer.shared.refreshService.registerBackgroundRefresh()
```

4. Update `Info.plist` to add:
   - Under `UIBackgroundModes` add `fetch`
   - Add entry for `BGTaskSchedulerPermittedIdentifiers` with array item `$(PRODUCT_BUNDLE_IDENTIFIER).refresh`

The bundle identifier suffix must match what you register in `RefreshService`.
  </action>
  <verify>
    <automated>grep -n "refreshService" DouyinLiveTV/App/DependencyContainer.swift && grep -n "BGTaskSchedulerPermittedIdentifiers" DouyinLiveTV/Info.plist</automated>
  </verify>
  <done>
    - DependencyContainer has `let refreshService: RefreshService` property
    - DependencyContainer initializes `RefreshService` with correct dependencies
    - DouyinLiveTVApp calls `registerBackgroundRefresh()` on launch
    - Info.plist has `BGTaskSchedulerPermittedIdentifiers` entry
    - Info.plist has `fetch` in `UIBackgroundModes`
  </done>
</task>

</tasks>

<verification>
Service follows existing patterns: manual DI, Combine for observation, async/await for network calls.
</verification>

<success_criteria>
- 30-minute timer runs for automatic refresh when app is in foreground
- Background refresh tasks are scheduled via BGTaskScheduler when app is in background
- Background refresh updates all favorite rooms with latest stats and live status in SwiftData
- All changes saved to persistent storage
- App launches without crashes
</success_criteria>

<output>
After completion, create `.planning/phases/06-refresh-quality-of-life/06-02-SUMMARY.md`
</output>
