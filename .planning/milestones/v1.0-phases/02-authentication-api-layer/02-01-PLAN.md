---
phase: 02-authentication-api-layer
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - DouyinLiveTV.xcodeproj/project.pbxproj
  - DouyinLiveTV/Domain/Models/AuthToken.swift
  - DouyinLiveTV/Domain/Models/LoginQRCode.swift
  - DouyinLiveTV/Data/Keychain/TokenStorage.swift
  - DouyinLiveTVTests/TokenStorageTests.swift
autonomous: true
requirements: [AUTH-02, AUTH-03]
user_setup: []

must_haves:
  truths:
    - "KeychainSwift dependency is added to the project via Swift Package Manager"
    - "AuthToken and LoginQRCode domain models exist with Codable conformance"
    - "TokenStorage can save tokens to Keychain"
    - "TokenStorage can load tokens from Keychain across app restarts"
    - "Tokens are never stored in UserDefaults or SwiftData"
  artifacts:
    - path: "DouyinLiveTV/Domain/Models/AuthToken.swift"
      provides: "Auth token model with accessToken, refreshToken, expiresAt"
      exports: ["struct AuthToken"]
      contains: "Codable"
    - path: "DouyinLiveTV/Domain/Models/LoginQRCode.swift"
      provides: "Login QR code response model"
      exports: ["struct LoginQRCode"]
    - path: "DouyinLiveTV/Data/Keychain/TokenStorage.swift"
      provides: "Keychain token storage wrapper using KeychainSwift"
      exports: ["class TokenStorage"]
    - path: "DouyinLiveTVTests/TokenStorageTests.swift"
      provides: "Unit tests for TokenStorage behavior"
  key_links:
    - from: "TokenStorage.swift"
      to: "KeychainSwift"
      via: "import and instantiation"
      pattern: "import KeychainSwift"
    - from: "TokenStorage"
      to: "Keychain"
      via: ".afterFirstUnlock accessibility for tvOS persistence"
      pattern: "afterFirstUnlock"
---

<objective>
Add KeychainSwift dependency, create authentication domain models, and implement secure token storage in Keychain.

Purpose: This provides the foundation for secure credential storage that AUTH-02 and AUTH-03 requirements depend on. Tokens must be encrypted in Keychain (never in plaintext) and persist across app restarts.
Output: KeychainSwift configured, domain models created, TokenStorage implementation with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-api-layer/02-CONTEXT.md
@.planning/phases/02-authentication-api-layer/02-RESEARCH.md
@DouyinLiveTV/App/DependencyContainer.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add KeychainSwift dependency to Xcode project via Swift Package Manager</name>
  <files>C:/Users/59213/douyin/DouyinLiveTV.xcodeproj/project.pbxproj</files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV.xcodeproj/project.pbxproj
  </read_first>
  <action>
    Add KeychainSwift (version 8.0.0 or later) to the Xcode project through Swift Package Manager. The repository URL is: https://github.com/evgenyneu/KeychainSwift.git

    This follows locked decision D-03 to use KeychainSwift instead of raw Keychain Services API.
  </action>
  <verify>
    <automated>grep -q "evgenyneu/KeychainSwift" C:/Users/59213/douyin/DouyinLiveTV.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - project.pbxproj contains package URL "https://github.com/evgenyneu/KeychainSwift.git"
    - product dependency for KeychainSwift is added to the DouyinLiveTV target
  </acceptance_criteria>
  <done>KeychainSwift dependency successfully added via Swift Package Manager</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create authentication domain models (AuthToken, LoginQRCode)</name>
  <files>
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/AuthToken.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LoginQRCode.swift
  </files>
  <read_first>
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveRoom.swift
    - C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LiveStats.swift
  </read_first>
  <action>
    Create two files in Domain/Models:

    1. AuthToken.swift: Define a struct AuthToken with:
       - accessToken: String
       - refreshToken: String
       - expiresAt: Date
       - Conform to Codable
       - Add import Foundation

    2. LoginQRCode.swift: Define a struct LoginQRCode with:
       - uuid: String
       - qrURL: String
       - expiresAt: Date
       - Conform to Codable
       - Add import Foundation

    Follow the same pattern as existing LiveRoom.swift and LiveStats.swift in the same directory.
  </action>
  <verify>
    <automated>ls C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/AuthToken.swift && ls C:/Users/59213/douyin/DouyinLiveTV/Domain/Models/LoginQRCode.swift</automated>
  </verify>
  <acceptance_criteria>
    - DouyinLiveTV/Domain/Models/AuthToken.swift exists
    - AuthToken contains "struct AuthToken: Codable"
    - AuthToken contains "let accessToken: String"
    - AuthToken contains "let refreshToken: String"
    - AuthToken contains "let expiresAt: Date"
    - DouyinLiveTV/Domain/Models/LoginQRCode.swift exists
    - LoginQRCode contains "struct LoginQRCode: Codable"
  </acceptance_criteria>
  <done>Both authentication domain models created with Codable conformance</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement TokenStorage for Keychain with unit tests</name>
  <files>
  - C:/Users/59213/douyin/DouyinLiveTV/Data/Keychain/TokenStorage.swift
  - C:/Users/59213/douyin/DouyinLiveTVTests/TokenStorageTests.swift
  </files>
  <behavior>
    - Test 1: save() stores tokens in Keychain with correct accessibility (afterFirstUnlock)
    - Test 2: load() returns nil when no tokens saved
    - Test 3: after saving a token, load() returns the same token with equal values
    - Test 4: clear() removes all tokens from Keychain
    - Test 5: hasValidTokens() returns true when token exists and not expired
    - Test 6: hasValidTokens() returns false when token is expired
  </behavior>
  <action>
    1. Create directory Data/Keychain/ if it doesn't exist
    2. Create TokenStorage.swift:
       - Import KeychainSwift and Foundation
       - Define public class TokenStorage
       - Private let keychain = KeychainSwift()
       - Define private keys: "com.douyinlivetv.accessToken", "com.douyinlivetv.refreshToken", "com.douyinlivetv.expiresAt"
       - Implement func save(_ token: AuthToken) throws
       - Implement func load() -> AuthToken?
       - Implement func clear()
       - Implement func hasValidTokens() -> Bool
       - When saving, use .afterFirstUnlock accessibility for tvOS to persist across reboots (per D-03, D-04)
       - Tokens MUST NOT be stored anywhere except Keychain (per D-04 — never UserDefaults or SwiftData)
    3. Create TokenStorageTests.swift in the test target with XCTest cases for all behaviors listed above
  </action>
  <verify>
    <automated>xcodebuild test -scheme DouyinLiveTV -destination 'platform=tvOS Simulator,name=Apple TV' -only-testing:DouyinLiveTVTests/TokenStorageTests</automated>
  </verify>
  <acceptance_criteria>
    - TokenStorage.swift exists at Data/Keychain/TokenStorage.swift
    - TokenStorage imports "import KeychainSwift"
    - save() uses ".afterFirstUnlock" accessibility for all three keys
    - Tokens are only stored in Keychain — no UserDefaults or SwiftData references
    - TokenStorageTests.swift exists with at least 6 test methods
    - All TokenStorageTests pass when run via xcodebuild
  </acceptance_criteria>
  <done>TokenStorage implemented with all required operations and passing tests</done>
</task>

</tasks>

<verification>
After this plan completes:
- All unit tests for TokenStorage pass
- KeychainSwift is properly linked
- Domain models follow project patterns
- Tokens can be saved, loaded, cleared, and validated
</verification>

<success_criteria>
1. KeychainSwift dependency added ✓
2. AuthToken and LoginQRCode models created ✓
3. TokenStorage saves to Keychain ✓
4. TokenStorage loads from Keychain ✓
5. TokenStorage clears from Keychain ✓
6. All unit tests pass ✓
7. Tokens stored only in Keychain (D-04 compliance) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-api-layer/02-01-SUMMARY.md`
</output>
